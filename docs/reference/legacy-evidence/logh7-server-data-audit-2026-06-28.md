# LOGH VII Server-Data Delivery Audit (2026-06-28)

> Playable-route addendum (2026-06-29): standard playable/live profile now opts into the interaction-unblock shims and static master deliveries: `LOGH_ACTION_LIST_CATEGORY=0`, `LOGH_COMMAND_TABLE_PRELOAD_PROBE=1`, `LOGH_STATIC_SHIPS=1`, `LOGH_STATIC_TROOPS=1`, `LOGH_STATIC_FIGHTERS=1`, `LOGH_STATIC_ARMS=1`, `LOGH_STATIC_POWER_DISTRIBUTION=1`, `LOGH_STATIC_MASTER_PLAYABLE_SEED=1`. Provenance split: ship master is existing manual/P1 `ship-stats.json`; troop master is manual/P1 `manual/troops-deployment.json`; command-card preload and fighter/arms/power fallback values are **P3 playable seeds**, not canon. If `manual/static-info-masters.json` or `static-info-masters.json` is recovered, `createInfoRecordsStaticState()` uses that ahead of P3 playable seeds. Purpose is to reveal client objects/interactions first, then replace temporary seeds with recovered original data.

Comprehensive audit of ALL data the server must provide to the client, by domain: content held ??delivered ??gaps.
Produced by the `logh7-server-data-audit` workflow (9 domains ??synthesis). Answers "Î¨¥Ïóá??Ï§òÏïº ?òÍ≥† Î¨¥Ïóá??Îπ†Ï°å??.
Provenance discipline: P0 original-recovered, P1 RE-derived, P2 manual/reconstructed, P3 invented gameplay seed.
DO NOT fabricate data or promote P2/P3 as original; respect the planetless-system rule (journals #53/#55).

> Current addendum (2026-06-29): the canonical galaxy roster is now **85 systems / 281 planets**. Only **80 systems** have recovered manual/raster coordinates and can be emitted as `0x0313`/`0x0315` grid markers. The five constmsg-P0, coordinate-pending systems are `?¢„É≥?¶„É¨?¨„É´??(13), `?±„Éº?ó„Éõ?º„É≥`(32), `?≥„Éñ?©„É¥?ß„É´??(34), `?ã„Éº?ô„É´?≥„Ç∞`(52), and `?¢„É≥?µ„É´?¥„Ç°?º„É´`(75). Keep them name-only until a lawful original-server capture/source appears; do not fabricate cells. Machine-readable current sweep: `RE/.omo/ghidra/data-sweep.json`.

## Highest-value DELIVERY gaps (server HAS data but zero-fills / gates-off / wrong target ??just wire it)
1. **DEFECT 2 ??planet/base panel "?§Ìèø Î∂àÎ™Ö"**: panel FUN_0057aa90 reads base NAMES from the static table fed only by 0x031d, which is emitted ONLY on the on-demand 0x031c PULL, not in the world-import PUSH ??empty until pulled. FIX (this cycle, gated LOGH_WORLD_IMPORT_STATIC_BASE): push 0x031d at world-import. (Economy scalars 0x0337 are a separate, opcode-collision-blocked half.)
2. **DEFECT 1 ??player enters as ?©Ï†ú/Emperor, stats 0**: conn3 world session has generatedCharacterId=0; with no LOGH_ACCOUNT_DB the created char isn't carried, so activeCharacterId() falls to worldCharId()=1 (Friedrich IV). World-entry 0x0323 also omits abilities (stats 0). FIX: thread created/charged char id into the conn3 session + deliver real abilities on 0x0323; persist accounts in the live path.
3. **Strategic fleets 0x0325**: 24 real fleets in worldState but default emit sends unitCount:1; all-fleets path gated by LOGH_STRAT_SIM/MP_VISIBILITY. FIX: send full fleet table on single-client world entry; use each fleet's scenario cell.
4. **Ship master 0x030b**: 63 real hulls gated OFF (LOGH_STATIC_SHIPS not in defaults) ??empty spec sheet. FIX: add to PLAYABLE_ENV_DEFAULTS after live A/B (LIVE_SAFE_STATIC_SHIP_CAP exists).
5. **Static catalogs 0x030d/0x030f/0x0311/0x0309/0x0307**: byte-exact builders exist but no request handler wired ??always zero-fill. BUT arms/fighters/troop/power CONTENT is not recovered ??keep count=0 (no fabrication); wire only where real content exists.
6. **Chat 0x0f1c GridChat endianness**: server parsed BE while client serializes LE ??Korean mojibake. FIXED this cycle (command-engine.mjs LE).
7. **Galaxy grid 0x0313/0x0315**: real positioned grid = 80 coordinate-recovered systems from the 85-system roster; even with flags the real cell grid can't reach the LIVE table (client run-once snapshot race FUN_004c5350 guard 0x2c03c0) ??needs a CLIENT-SIDE binary patch (A0 backlog), not a server change. The remaining 5 systems are name-only coordinate-pending, not emit candidates.

## CONTENT gaps (data missing from content ??needs recovery/RE; DO NOT fabricate)
- Arms hit table arms[27][8] (0x0311), fighter master (0x030f), ground-troop class master (0x030d), power-distribution curves (0x0309) ??no content file; recover from EXE static dump / manual RE.
- Per-planet economy scalars (population/food/?? ??only P3 procedural (planet-economy.json); no P0/P1 source. 0x031f scalar offsets PROVISIONAL ??keep 0.
- Card factory-id mapping for 0x0305/0x0307 command cards ??not recovered (only P3 probe ids 0x002b/0x0041).
- Officer numeric stats ??P2 (IV/VI-derived), NOT VII-original; only 12 face_index anchors are P0.
- Lobby session powers/score (d0/d1/d2) + world_json ??P3 stub; no P0/P1 source.

---


## galaxy-systems-stars (star systems + strategic galaxy map: positions, markers, spectral class; grid-type/grid/grid-selector records 0x0312/0x0313, 0x0314/0x0315, 0x0316/0x0317)
- status: **gated-off**
- gates: LOGH_STRAT_GALAXY (stratGalaxyEnabled, logh7-login-session.mjs:356) default OFF -- master switch for injecting the 80-positioned-system grid from the 85-system roster | LOGH_STRAT_GRID_EARLY (stratGridEarlyEnabled, :357) default OFF -- REQUIRED for the only branch that reaches the LIVE strategic table (1842); without it the real grid is disabled because a non-empty 0x0315 at 0x0314 STALLS the world-init walk (G210b, comment 1835-1841) | LOGH_WORLD_PLAYER (worldPlayerEnabled, :197) default OFF -- required by BOTH the early grid branch (1833) and the 0x0f02 push (1664) | LOGH_STRAT_TERRAIN (stratTerrainEnabled, :319) default OFF -- adds navigable-space/plasma/sargasso terrain so cells are actually move-into-able | LOGH_STRAT_GRID (stratGridEnabled, :314) default OFF -- bare empty 100x50 grid fallback | LOGH_STRAT_FLEET (stratFleetEnabled, :325) default OFF -- fleet-only board fallback | LOGH_STRAT_GRID_OBJECT_PRELOAD (:358-360) default = follows GRID_EARLY | LOGH_GRID_SELECTOR_PROBE (:270) default OFF / LOGH_GRID_SELECTOR_VALUE (:272) -- 0x0317 selector lever, probe only | LOGH_SEED_CANON_NPCS (seedCanonNpcsEnabled, :201) default OFF -- without it the lone player HUD falls back to '?áÂ∏ù'(emperor) placeholder | NET: real galaxy map needs LOGH_STRAT_GALAXY=1 AND LOGH_STRAT_GRID_EARLY=1 AND LOGH_WORLD_PLAYER=1 simultaneously; all three default OFF, so default delivery is empty. The logh7-live SKILL.md run command (:27-28) sets all of these, i.e. real delivery only happens under the explicit live harness, not stock server defaults.
- deliveryGaps:
  - BY DEFAULT the strategic galaxy map is EMPTY: 0x0314->0x0315 and 0x0312->0x0313 are answered by the generic zero-fill walker (buildWorldDataResponseInner, count=0). Client RLE decoder FUN_004abbb0 (redex-confirmed) requires rleCount!=0 && rleCount<0x1389 and returns WITHOUT writing for rleCount=0, so the 80 recovered positions/markers/spectral are never painted -- the server HAS the positioned grid but does not send it.
  - Even with flags on, the real cell grid CANNOT reach the LIVE table via the network: a non-empty 0x0315 at 0x0314 stalls the world-init walk (G210b, 1835-1841) and the 0x0f02 push (1682-1709) arrives AFTER the scene-timed run-once snapshot (FUN_004c5350 guard 0x2c03c0), so live shows ~27 scattered garbage cells. Closing this needs a CLIENT-SIDE guard-clear binary patch (docs A0 backlog), not a server change.
  - 0x0316/0x0317 grid-selector: canon server sends no real ResponseInformationGrid; 0x0317 is OK-status only. The selector dword (clientBase+0x35f358/35f35a) is left unset -> client mode branch (FUN_004b68f0) defaults; only an opt-in probe touches it. Grid-selector data is effectively missing.
  - LIVE defect (planet panel '?§Ìè¨Î∂àÎ™Ö' for Valhalla): the selected-system / planet info panel is the 0x031f base-record domain (logh7-base-record.mjs systemToBaseRecord) -- adjacent to this domain. galaxy.json carries planets[] (orbit order) but the system-selection -> planet-panel painting is not delivered for the selected system by default, so the panel shows spot-unknown. (Strictly a 0x031f/base-economy delivery gap bordering galaxy-systems-stars; planetless systems legitimately have empty planets[].)
  - LIVE defect (player enters as '?áÂ∏ù' with stats 0): the created character is not delivered as the authoritative player 0x0323 record by default (gated by LOGH_WORLD_PLAYER / LOGH_SEED_CANON_NPCS, both OFF), so the lone unit HUD falls back to the emperor placeholder. This is the player-record/world-entry domain, but it co-occurs with the galaxy map being empty in the same default world entry.
- contentGaps:
  - Special-body (black hole / neutron star) CELL placement: content has count=3+3 (P1) but NO canon system->cell mapping (P3). Builder only emits them if caller passes blackHoleCells/neutronStarCells, and no content file supplies them -> they are never placed. Do not fabricate cells.
  - Sargasso obstacle cells: no content file (galaxy-sargasso-cells.json absent); only a procedural/fallback heuristic near the Iserlohn corridor exists (P3 server convention, not canon).
  - Plasma-storm cells: no canon positions exist; generated procedurally by deterministic seed (generatePlasmaCells) = P3 by design (manual p31 says positions are not fixed).
  - Original-server star coordinates and original stellar (spectral) grades are NOT recovered -- current cx/cy + spectralClass are P1 raster-derived; the chart contains no 'O'-class star, so any O-class marker variant slot is unused.
  - Dedicated fortress map coordinates: fortresses reuse the parent-system marker cell (provenance doc: 'parent-system-marker-not-dedicated-fortress-coordinate'), no separate fortress dot is recovered.
- rec: Do NOT fabricate coordinates or promote P1/P2/P3 to original. The content side now has 85 system names, 281 planets, and 80 coordinate-recovered grid markers; the problem is delivery + a client snapshot race plus the separate 5-system coordinate-loss gap. Minimal evidence-justified steps: (1) Make the real positioned galaxy grid the DEFAULT for live world entry by enabling the existing branch -- i.e. flip the live launch to always set LOGH_STRAT_GALAXY + LOGH_WORLD_PLAYER (already done in logh7-live SKILL.md:27-28) and treat that as the canonical server profile; document that stock defaults intentionally ship empty for the legacy single-record tests. (2) The real cell grid (0x0315) reaching the LIVE table is blocked by the scene-timed run-once snapshot, NOT by content -- the only correct fix is the client-side guard-clear binary patch (clear clientBase+0x2c03c0 after the 0x0f02 push so FUN_004c5350 re-runs), tracked in docs/logh7-inworld-backlog.md A0; keep the server's 1831-1856 + 1682-1709 paths ready for it and verify live per logh7-live. (3) Special bodies/sargasso and the five coordinate-pending systems: leave as count/name-only until a canon system->cell mapping is recovered; do not invent cells. (4) For the '?§Ìè¨Î∂àÎ™Ö' planet panel and '?áÂ∏ù' placeholder, address them in their own domains (0x031f base record selection + 0x0323 player record / LOGH_WORLD_PLAYER+LOGH_SEED_CANON_NPCS), not by changing galaxy.json. Respect the planetless-system rule: an empty planet panel for a genuinely planetless system is correct, but Valhalla is not planetless so its empty panel is a real delivery gap.

## planets-bases-economy (?âÏÑ±/Í∏∞Ï?/Í≤ΩÏ†ú ??base panels: static master 0x031d, defense/dev/owner 0x031f, institution 0x0321, economy-scalar NotifyBaseParameter)
- status: **partial**
- gates: LOGH_CONTENT_DB default '1' (logh7-config.mjs:73) ??content source = DB built from galaxy.json | LOGH_PLANET_BASE_RECORDS default '1' (PLAYABLE_ENV_DEFAULTS logh7-config.mjs:60; check logh7-login-session.mjs:1372 !=='0') ??planets/fortresses surfaced as selectable bases in 0x031d/0x031f lists | LOGH_BASE_ECONOMY: NOT in PLAYABLE_ENV_DEFAULTS; check logh7-login-session.mjs:218 (!=='0') => effectively ON by default ??fills the 3 P3 array slots in 0x031f | LOGH_PROVISIONAL_BASE_PARAMETER_NOTIFY: NOT in defaults; check logh7-login-session.mjs:221 (==='1') => OFF by default. This single gate is what suppresses the entire economy-SCALAR record (‰∫∫Âè£/È£üÊñô/Ê≤ªÂÆâ/etc.) because opcode 0x0337 collides with ResponseTacticsCharacter (RE 2026-06-28, base-economy.mjs:65-67,219-220). baseParamForSpot returns null unless BOTH baseEconomy AND this are on (logh7-login-session.mjs:1516). | LOGH_KO_NAMES default '1' (logh7-config.mjs:74) ??0x031d/base names emitted in Korean | LOGH_WORLD_PLAYER default '1' (logh7-config.mjs:49) ??gates the 0x0f02 PUSH path that ships base/institution records; FACTION_CAPITAL.empire.spot=70 (logh7-login-session.mjs:341) = staticBaseRecords index69 = ?¥„Ç°?´„Éè??Î∞úÌï†?? | LOGH_POSTLOAD_PLAYER_RECORD/LOGH_POSTLOAD_RICH_CHARACTER default '1' (logh7-config.mjs:50-51) ??adjacent player-record domain; world-init 0x0323 player record is intentionally MINIMAL (logh7-login-session.mjs:1716-1738, 'Rich fields remain post-load only') which is the player-stats-0 / ?©Ï†ú-fallback root, distinct from this domain's planet-panel-empty cause | applyEnvDefaults applied at logh7-server.mjs:807 ??confirmed resolved zero-config: worldPlayer=1, planetBaseRecords=1, baseEconomy effective ON, baseParameterNotify OFF, contentDb=1
- deliveryGaps:
  - NotifyBaseParameter economy-SCALAR record (‰∫∫Âè£/È£üÊñô/Ê≤ªÂÆâ/?ùÊÉ≥/ÂÆóÊïô/??åÅ????population@0x28, food@0x40 CONFIRMED) is built and ready (logh7-base-economy.mjs + baseParamForSpot) but NEVER SENT by default: gated by LOGH_PROVISIONAL_BASE_PARAMETER_NOTIFY (default OFF) AND opcode 0x0337 collides with live ResponseTacticsCharacter. THIS is the pinned root of the empty planet/base economy panel ('?§Ìèø Î∂àÎ™Ö' / no data painted for Î∞úÌï†??: the 0x031f record delivered carries owner/class + 3 array slots but ZERO for every displayed scalar, and the record that would carry those scalars is suppressed. No correct opcode is known, so it cannot simply be turned on.
  - 0x031f scalar fields (population/food/approval/peace/...) are intentionally left 0 even with LOGH_BASE_ECONOMY ON (codec/base-record.mjs:319-355, economyBaseRecord array-only) because their absolute byte offsets are PROVISIONAL ??so the defense/economy panel paints owner+class only, columns empty.
  - 0x0321 institution record ships only 1 seeded facility with scalar fields 0 (P3) ??facilities panel near-empty.
  - 0x0327 warehouse (supplies/food/mineral) and 0x0329 package economy values left 0 (logh7-login-session.mjs:2376-2398) ??stockpile panels empty by design (no fabrication).
  - Player world record at 0x0f02 (0x0323) is minimal name/rank/power only (stats not delivered) ??live ?©Ï†ú-placeholder-with-stats-0; this is the player-record/postload domain, adjacent, not the planet-economy delivery itself.
- contentGaps:
  - DB planets table has NO economy columns (only id/system_id/name_ja/orbit) ??population/food/industry/habitable exist ONLY in planet-economy.json which is procedural P3, not recovered P0. There is no P0/P1 source for per-planet economy scalars anywhere in content.
  - No P0 per-planet ownership/development/garrison/mineral/tax values ??the 0x031f scalar half and the NotifyBaseParameter scalar half are sourced only from the P3 procedural pool or left 0; do NOT fabricate.
  - planet orbit values are P2-reconstructed (galaxy.json _planet_order), not original P0 game data.
  - ivex_planets (97, higher provenance) carry name only, no orbit/economy ??cannot fill the gap.
  - Systems carry no canonical id/grid in content; ids are synthesized (index+1), so spot<->base binding is a server convention, not recovered data.
- rec: Do not fabricate or promote P3 to original. Three evidence-justified steps, all respecting the planetless-system rule (skip ?û„É≠?º„É¥?£„Ç¢/?ï„Ç©?´„Çª?Ü„Ç£/?§„Çº?´„É≠?º„É≥ which legitimately have no planets, journals #53/#55): (1) The pinned planet-panel-empty defect is that the NotifyBaseParameter economy-scalar record cannot be delivered because opcode 0x0337 collides with ResponseTacticsCharacter. Resolve the correct opcode first via RE (Input_NotifyBaseParameter::input_from_stream @0x764aa0 has a parser but no dispatcher 'OK' route ??confirm via logh7-re/redex whether the client ever dispatches it; if it does not, the scalar panel is genuinely populated by a different record/path and the fix is to find that route, NOT to keep emitting 0x0337). Only after the route is RE-pinned, gate-on and verify live with logh7-live on Î∞úÌï†??spot 70. (2) Until then, label the scalar values P3 (procedural) in any UI and keep the gate OFF rather than emitting on the colliding opcode. (3) For 0x031f scalar fields, leave at 0 until their absolute offsets are RE-confirmed (currently PROVISIONAL) ??do not write P3 numbers onto unconfirmed offsets. The 3 array slots already shipped (budget/commodity/budgeting) should stay tagged P3-proxy. The player-stats-0/?©Ï†ú issue is a separate player-record/postload domain (enable/verify LOGH_POSTLOAD_RICH_CHARACTER delivery), not part of this fix.

## characters-officers-roster
- status: **partial**
- gates: LOGH_CONTENT_DB default '1' (logh7-config.mjs:73; PLAYABLE_ENV_DEFAULTS) ??loads the 97-char pack incl. char id=1 = ?©Ï†ú | LOGH_WORLD_PLAYER default '1' in playable boot (logh7-config.mjs:49) but raw default OFF (login-session.mjs:197 requires ==='1') ??gates the world-entry player 0x0323 | LOGH_POSTLOAD_PLAYER_RECORD default '1' (config.mjs:50) | LOGH_POSTLOAD_RICH_CHARACTER default '1' (config.mjs:51) ??the ONLY path that ships player abilities/stamina/face at world entry; if off the player card is statless | LOGH_SEED_CANON_NPCS default OFF (login-session.mjs:201; NOT in PLAYABLE_ENV_DEFAULTS; live skill SKILL.md does NOT set it) despite code comment claiming 'Í∏∞Î≥∏ ON' ??so the NPC hierarchy that would stop the lone-player ?©Ï†ú fallback is NOT seeded by default | LOGH_ROSTER_PUSH default OFF (login-session.mjs:248) | LOGH_PRESEED_PLAYER_CHAR default OFF (login-session.mjs:1292; live skill keeps it off) | LOGH_WORLD_CHAR_ID default 1 (login-session.mjs:117) ??autologin/default player resolves to char 1 = Friedrich IV | ability-seed (resolveCreatedAbilities) is UNGATED ??always stamps origin BASE on created chars (login-session.mjs:2495)
- deliveryGaps:
  - DEFECT 1 (P0, live journal #66): player enters as ?©Ï†ú with ALL stats 0. Root cause is a delivery+binding gap: (a) the world-entry player 0x0323 at login-session.mjs:1720 deliberately omits abilities/stamina/face, so even the correct player paints stats 0 until the post-load 0x0356 refresh; (b) when no created char is bound (conn3 world session starts with generatedCharacterId=0, rebind only happens if account+persisted profile via loadAccountProfileCharacters login-session.mjs:825-843), activeCharacterId() falls to worldCharId()=1 = contentPack char 1 = Friedrich IV (?áÂ∏ù). The synthFallbackPlayer guard (login-session.mjs:1073, name '?†ÏûÑ ?¨Í?') only fires when hasRealPlayerChar()===false; if account char>0 or LOGH_WORLD_CHAR_ID is set, hasRealPlayerChar()=true and activeCharacterRecord(1) returns the Emperor unmasked ??exactly the live ?©Ï†ú. Trace shows 0x0323x26 sent yet BL panel still ?©Ï†ú/stats 0.
  - The created character IS captured correctly (login-session.mjs:2499-2540: abilities seeded, stamina=100, title clamped, generatedCharacterId set) but is NOT reliably threaded into the separate world-entry session ??binding depends on account profile persistence (saveGeneratedProfile needs accountStore.addProfileCharacter), a fragile cross-session handoff.
  - World-entry player 0x0323 sends title:null and face omitted by design (login-session.mjs:1736,1720) ??player face/peerage never reach the in-world HUD at entry (only via info-panel 0x0322 read or post-load).
  - Canon NPC hierarchy 0x0323 seed exists (login-session.mjs:1743-1766) but is gated OFF by default (LOGH_SEED_CANON_NPCS) and not set in the live launch config, so the world is empty of NPC officers and the lone player is more likely to inherit the ?©Ï†ú apex slot.
  - NPC display names are masked to 'Character N' unless manualDocumented (login-session.mjs:1136-1142) ??correct P-tag discipline but means most seeded NPCs ship anonymized even when seeding IS enabled.
- contentGaps:
  - NO VII-original numeric officer stats exist: the only P0 source (st_char.html) has stats:null. All 8-ability blocks in characters.json/DB are P2 (IV/VI-EX-derived) ??must NOT be presented as VII-original
  - Only 12 of ~97 characters have a P0-confirmed face_index; the other ~85 carry P2/P3 (AI-classified) portrait/face guesses (face-name-map _total_recovered=12, face_pool anchors only [85,206])
  - No canon player-created-character template (the created player is a P3 gameplay seed: stamina=STAMINA_FULL 100, abilities from P3 ability-seed.json, title clamped to commoner)
  - No content backing for the live-observed planet panel for Î∞úÌï†??Valhalla beyond systems that actually have planets (respect planetless-system rule, journals #53/#55) ??out of this domain but coupled to the same world-entry record burst
- rec: Fix DEFECT 1 by binding the created character as the in-world player and giving its 0x0323 real stats, without fabricating data: (1) Thread the created/charged char id into the world-entry session deterministically (do not rely solely on account-profile persistence): ensure activeCharacterId() resolves to generatedCharacterId/chargedCharacterId on the conn3 world session, e.g. pass the selected char id from session-select into the world session, so it never falls to worldCharId()=1=Friedrich IV. (2) When the player is genuinely unbound, make the synthFallbackPlayer path authoritative (it already replaces ?©Ï†ú with ?†ÏûÑ ?¨Í? / commoner title 0) by ensuring hasRealPlayerChar() is FALSE in that case (do not set LOGH_WORLD_CHAR_ID for autologin) ??the existing guard is correct, the live config just lets account.char=1 slip through. (3) Include the player's real abilities + characterHudFields (stamina) on the world-entry 0x0323 (login-session.mjs:1720) instead of deferring everything to the post-load 0x0356, so stats are non-zero immediately; values come from the created char (P3 seed is legitimate for a created PC) or synthFallback. (4) Keep titlename/peerage out of the name slot (current title:null is correct to avoid the HUD '?©Ï†ú' name fallback). Do NOT present P2/P3 stats or AI portrait guesses as VII-original; keep only the 12 face_index anchors as P0. Respect the planetless-system rule for the coupled planet-panel defect.

## fleets-units-ships
- status: **partial**
- gates: LOGH_FULL_UNIT_LOCATION default '1' (in PLAYABLE_ENV_DEFAULTS logh7-config.mjs:56) ??ON: 0x0325 carries one FULL player fleet element; OFF: id+count stub only | LOGH_WORLD_PLAYER default '1' (config.mjs:49) ??world-entry player anchor ON | LOGH_POSTLOAD_PLAYER_RECORD default '1' (config.mjs:50) ??postload re-push of player record+unit ON | LOGH_POSTLOAD_UNIT_STREAM_WIRE default '1' (config.mjs:54) ??parser-stream BE wire for postload 0x0325 | LOGH_SCENARIO default 'content/scenarios/canon-801-07.json' (config.mjs:77) ??seeds 24 fleets into worldState via upsertFleet (logh7-scenario.mjs:85-89) | LOGH_STATIC_SHIPS ??NOT in PLAYABLE_ENV_DEFAULTS => DEFAULT OFF: 0x030b ship master falls through to walker zero-fill (count=0), so ship classes/stats are NOT sent | LOGH_STRAT_SIM ??NOT in defaults (asBool default false; logh7-config.mjs:160) => DEFAULT OFF: the all-fleets 0x0325 broadcast never runs | LOGH_MP_VISIBILITY ??NOT in defaults (config.mjs:144) => DEFAULT OFF: distinct MP fleets are never pushed/registered (only upsertCharacter runs) | LOGH_NPC_SEED / LOGH_NPC_AI ??NOT in defaults (config.mjs:145-146) => OFF: no NPC fleets seeded beyond the scenario | LOGH_PLAYER_FOCUS_CELL default '1' (config.mjs:55) ??overrides commander slot with focus cell id | LOGH_WORLD_CHAR_ID default 1 (logh7-login-session.mjs:117) ??drives the ?©Ï†ú-fallback root when no real char | LOGH_PLANET_BASE_RECORDS default '1' (config.mjs:60), LOGH_BASE_ECONOMY default-ON unless '0' ??relevant to the unit's spotResolverBase->HUD spot resolution
- deliveryGaps:
  - 24 real fleets sit in worldState (seeded from canon-801-07.json) but are NEVER delivered by default: every default 0x0325 emit sends unitCount:1 (player's own fleet only) at logh7-login-session.mjs:2347-2352, 1710-1715, 1919-1925. The all-fleets path (auth-server.mjs:1572-1578 listFleets() broadcast) is gated by LOGH_STRAT_SIM=OFF, and the MP push (auth-server.mjs:1885-1893) by LOGH_MP_VISIBILITY=OFF. So strategic fleets are HAVE-but-not-sent.
  - Player's own delivered fleet uses cell=fleetCellId() (env/capital-derived, logh7-login-session.mjs:1345/1246-1248) instead of the player fleet's scenario cell ??wrong/seeded position even for the one fleet that IS sent.
  - 0x030b ship-class master (63 real hulls in state.shipClasses) is gated OFF by default (LOGH_STATIC_SHIPS not in defaults); the walker answers 0x030b with count=0 => ship classes/stats panel paints empty unless the flag is set.
  - 0x030d ground-troop master: builder buildStaticInformationUnitTroopInner exists and is byte-correct but is never imported or wired (no 0x030c handler in login-session) => answered ONLY by buildWorldDataResponseInner zero-fill (size 0x0184) => troop class panel EMPTY by default.
  - LIVE DEFECT (Î∞úÌï†??'spot Î∂àÎ™Ö'): the unit's spotResolverBase/mapSection=location.spot (logh7-login-session.mjs:1351-1352) feeds FUN_004c9170 HUD spot resolution, but the planet/base record for that spot is not painted (planet-base domain), so the fleet's HUD location reads '?§Ìèø Î∂àÎ™Ö'.
  - LIVE DEFECT (player enters as ?©Ï†ú, stats 0): created character (0x1008) sets generatedCharacterId in the LOBBY session, but conn3/world session is a separate instance (logh7-login-session.mjs:825-842 note) so hasRealPlayerChar() can be false at world-entry; activeCharacterId() falls back to worldCharId()=1 (canon sovereign). synthFallbackPlayer ('?†ÏûÑ ?¨Í?', logh7-login-session.mjs:1073-1091) is the intended mitigation but only fires when contentPack returns the canon rec AND no lobby/charged/account/env char ??the created char is not carried into the world session, so the real created stats are not delivered as the player 0x0323 record.
- contentGaps:
  - No per-fleet ship-unit roster: all 24 scenario fleets have boats:[] (0/24), so the 0x0325 troop_units/officer region (B+0x14/B+0x18, client cap 10) has nothing real to send ??fleet ship/officer composition is content-missing (would be P3 if invented; do NOT fabricate)
  - No ground-troop CLASS master content for 0x030d ResponseStaticInformationUnitTroop (client cap 16, str 0x00762f68): info-records-static state.troops=[] (logh7-info-records-static.mjs:735) and scenario troops[] are battlefield instances, not the 16-class master table
  - No live unit<->ship-class binding: scenario ships[] carry shipClass strings ('battleship') but the 0x0325 unit record has no ship-class field and boats[] is empty, so a delivered fleet cannot be tied to its 0x030b hull stats
  - Scenario fleet cell positions are P2 (galaxy.json canon dot projection), not P0 recovered original fleet coordinates ??acceptable as P2 but must not be promoted to original
- rec: Deliver the real strategic fleets that already exist in worldState without inventing data: (1) In the default world-entry/postload 0x0325 emit, when worldState has fleets, send the FULL table via buildInformationUnitRecordInner({wireLayout:'parser-stream', fleets: worldState.listFleets()}) instead of the hard-coded unitCount:1 ??reuse the exact path already proven at logh7-auth-server.mjs:1573-1576, but make it run on single-client world-entry (not only under LOGH_STRAT_SIM/LOGH_MP_VISIBILITY). Keep the player's own fleet's id == char flagship@0x24 binding so local spawn (FUN_004c2a80, info_size cap 600 @0x763848) still anchors. (2) Use each fleet's scenario cell (already row*100+col, P2) for the cell@B+0x0c slot rather than fleetCellId(), so fleets render at their canon positions; the boats region stays empty (no fabrication ??content genuinely lacks per-fleet rosters). (3) Add LOGH_STATIC_SHIPS to PLAYABLE_ENV_DEFAULTS (or wire 0x030a->0x030b unconditionally with the 63 P1 hulls and the documented ??00/name_len??3 client caps) so ship classes/stats actually reach the spec sheet. (4) Wire a 0x030c->0x030d handler calling the existing buildStaticInformationUnitTroopInner once a real ??6-class troop master exists (do NOT ship invented classes). (5) Fix the player-record carry: persist the 0x1008-created character into the conn3 world session (or set generatedCharacterId from the newest saved profile, as the existing logh7-login-session.mjs:825-842 path attempts) so the created character's real stats are delivered as the player 0x0323 record instead of falling back to canon char 1 / synth zero-stat. Respect the planetless-system rule: do not synthesize planet/base records for systems that legitimately have none (journals #53/#55) ??the Î∞úÌï†??spot fix belongs to the planet-base domain, not here.

## tactics-battle-data (tactical screen: 0x033b UnitShip / 0x033f UnitTroop+Corps / 0x0337 ResponseTacticsCharacter / 0x0349 PositionUnit / 0x0311 Arms / 0x030f Fighters / 0x040c CommandControl)
- status: **zero-filled**
- gates: LOGH_TACTICS_UNIT (tacticsUnitEnabled, logh7-login-session.mjs:504) -- DEFAULT OFF; when ON pushes only a single-unit 0x033b seed at world-init, still no battle stats/character/corps. | LOGH_BATTLE_ENTRY_PROBE (battleEntryProbeEnabled, logh7-login-session.mjs:278) -- DEFAULT OFF; the ONLY default path that would call openBattleField and emit the full real tactical push at login-session.mjs:1989. | LOGH_WORLD_PLAYER (worldPlayerEnabled, logh7-login-session.mjs:197) -- DEFAULT OFF; gates the world-entry character/unit record block (logh7-login-session.mjs:1664). | LOGH_SEED_CANON_NPCS (seedCanonNpcsEnabled, line 201) -- DEFAULT OFF. | LOGH_EARLY_WORLD_LOCATION (earlyWorldLocationEnabled, line 222) -- DEFAULT OFF; gates real spot/spotOwner in character records (the planet-panel target). | LOGH_GRID_ENTER (gridEnterEnabled, line 257) -- DEFAULT OFF. | LOGH_STATIC_SHIPS (line 173/2406) -- DEFAULT OFF; even when ON only feeds 0x030b ship MASTER, not 0x0311 arms / 0x030f fighters / 0x033b tactics. | No LOGH_* flag exists that wires real arms (0x0311) or fighters (0x030f) tables at all.
- deliveryGaps:
  - 0x033b/0x033f/0x0337/0x0349/0x0341/0x0343/0x0345 are ALL answered ONLY by the generic zero-fill walker by default (count=0) -> tactical screen paints EMPTY. Live-confirmed: journal #61/#63 + loop-state.md note 0x033b is 'generic-walker-answered' in the live world-entry trace. The real battle-engine builders exist but are unreachable without LOGH_BATTLE_ENTRY_PROBE=1 (default OFF).
  - 0x0311 Arms: byte-exact builder buildStaticInformationArmsInner exists (logh7-info-records-static.mjs:400) but has NO request handler -- the server HAS the encoder yet never sends it; always zero-fill. Pure delivery gap (only blocked by missing content + missing wiring).
  - 0x030f Fighters: same -- buildStaticInformationFightersInner (logh7-info-records-static.mjs:373) exists but is never invoked; always zero-fill.
  - DEFECT 1 (player=Emperor stats 0): the created character is not delivered as the in-world player record; world entry shows faction-leader/default placeholder with all leadership stats ZERO (loop-state.md:5, journal #66). The player/commander record that the tactical+command panels read is not threaded from the created char id -- and it is gated behind LOGH_WORLD_PLAYER (default OFF).
  - DEFECT 2 (planet panel ?§Ìèø Î∂àÎ™Ö for Valhalla): base records 0x031c/0x031d/0x031f sent once but the planet/orbit 'spot' panel stays empty; spot@0x1c=0 in 0x0323 (docs/logh7-loop-cycle-2026-06-21-endpoint-render.md:26). Real spot is gated behind LOGH_EARLY_WORLD_LOCATION (default OFF); spot panel may also need a record the walker zero-fills.
  - tacticsUnitEnabled path (login-session.mjs:1772) even when ON sends a degenerate single-unit 0x033b with no cross-matched 0x0337 character/0x033f corps, so the pool stays unpopulated (FUN_004c32a0 also hard-gates on mode byte 0x126711==0 which no server message sets -- logh7-login-protocol.mjs:1556-1559).
- contentGaps:
  - Weapon hit/spread table arms[27][8] (0x0311) -- NO content file holds it; builder needs 27x8 u16 hit values; would require RE-recovery from EXE/manual (do NOT fabricate).
  - Fighter/spartanian master (0x030f) -- NO content for kind/airbattle/antiship/defence/cruising per fighter type; not in content DB or any json.
  - Per-ship tactical battle state for 0x033b (the 52-byte record: shipId/HP/shield/beam/position fields are P2 unlabeled per journal #63) -- field semantics not recovered, so no authored values exist even though ship-stats.json/ship_classes.stats hold pools that could partially source them.
  - Corps (0x033f) morale/confusion/per-facing-shield aggregate -- no content.
  - PositionUnit (0x0349) battlefield coordinates and Obstacle (0x0347) hazard tables -- no authored battlefield/encounter content; only procedural center/scale grid placement exists.
  - Per-system planet/orbit detail for the spot panel (DEFECT 2 root): planets table has 281 rows but planetless-system rule (journals #53/#55) means some systems legitimately have none; Valhalla/Odin needs its real planet rows verified before delivery, not fabricated.
- rec: Do NOT fabricate tactical data. Phased, evidence-justified delivery: (1) WIRING-ONLY, no new content needed first: add request handlers so 0x0310->0x0311 Arms and 0x030e->0x030f Fighters call the existing byte-exact builders -- but only once the arms[27][8] hit table and fighter master are RE-recovered from the EXE/manual (P0/P1); until recovered, keep zero-fill (no guessing). (2) For 0x033b/0x0337/0x033f tactical screen: the real path already exists via openBattleField; the gap is that it is gated OFF (LOGH_BATTLE_ENTRY_PROBE default OFF) and the client populator hard-gates on mode byte clientBase+0x126711==0 (logh7-login-protocol.mjs:1556) which no server message sets -- so resolving this needs the live mode-poke experiment (journal #63 next item) before defaults change; do not promote procedural seed values (P3) to 'real'. (3) DEFECT 1: thread the created-character id into the world player record and flip LOGH_WORLD_PLAYER on by default once verified (player-not-emperor-fix doc 2026-06-26 has the binding logic); this is the highest-value fix and is delivery-only (the character content already exists, roster=75/characters=97). (4) DEFECT 2: deliver real planet/orbit rows for systems that HAVE planets (planets table, 281 rows) via the spot-targeted record, honoring the planetless-system rule (journals #53/#55) -- verify Valhalla/Odin actually has planet rows before sending; default-enable LOGH_EARLY_WORLD_LOCATION only after live-verifying the spot panel paints. Field semantics for the 0x033b 52-byte record remain P2 -- live-trace label before sourcing from ship_classes.stats.

## command-tables-actions (command-menu card 0x0305/0x0307 + action list 0x0356; gates user commands C002)
2026-06-29 addendum: `LOGH_POSTLOAD_ACTION_LIST_SEATS=1` is now standard
playable/live env, alongside `LOGH_POSTLOAD_PLAYER_RECORD=1`,
`LOGH_POSTLOAD_RICH_CHARACTER=1`, and `LOGH_WORLD_PLAYER=1`. This fixes the
action-list payload delivery part of C002 (`payload+0x270`, slot `0x67`) but
does not solve the remaining client-side widget activation gate.

- status: **zero-filled**
- gates: LOGH_COMMAND_TABLE_PRELOAD_PROBE ??default OFF (login-session.mjs:524). OFF => 0x0305/0x0307 answered by zero-fill walker (count 0). ON => single hardcoded card 0 with commands [0x002b,0x0041] (P3 probe, not canon factory ids). | LOGH_ACTION_LIST_SEATS ??default OFF (login-session.mjs:223). Gates world-init 0x0323 seat entries. | LOGH_POSTLOAD_ACTION_LIST_SEATS ??default OFF (:224). Gates post-load 0x0356 seat delta (proven to set client payload+0x270/slot 0x67). | LOGH_ACTION_LIST_CATEGORY ??default unset/null (:225-229). When set, injects a category dword seat (A/B discriminator only; not proven to change client tab/mode). | LOGH_C002_OFFICER_COUNT ??default unset/null (:231-235). When set (0..16) overrides seat-entry count. | LOGH_ACTION_LIST_APPOINTMENT ??default OFF (:230). Gates experimental 0x0707 CardAppointment. | LOGH_POSTLOAD_RICH_CHARACTER / LOGH_POSTLOAD_PLAYER_RECORD ??default OFF (:300-303). Gate the post-load 0x0323/0x0356 rich player record + seats. | LOGH_WORLD_PLAYER ??default OFF (:197). Gates the whole world-player seeding path that carries seatEntries. | LOGH_SEED_CANON_NPCS ??default OFF (:201; comment claims 'ON' but code requires '1'). Gates canon NPC hierarchy seed that prevents the ?©Ï†ú fallback.
- deliveryGaps:
  - 0x0305/0x0307 default world-load is zero-filled (outer count 0) via buildWorldDataResponseInner ??the command-menu card master is delivered EMPTY by default (login-protocol.mjs:1644-1654; test logh7-login-session.test.mjs:2165-2188 asserts default grid-enter does NOT include 0x0305). A record answered only by the generic walker = panel paints empty.
  - The real CP/command content the server already holds (81 strategy commands in strategy-commands.json; job-authority cards in cards-cp-orgs.json) is NEVER projected into buildStaticInformationCardInner. The data is loaded only for CP-cost (logh7-command-cost.mjs) and the content-DB strategy_commands table (logh7-content-db.mjs:233); it does not reach the 0x0305 builder. Server HAS command content but does not send it as the card table.
  - The populated card builder is reachable only behind LOGH_COMMAND_TABLE_PRELOAD_PROBE (default OFF) and even then emits a single P3 probe card (card 0, commands 0x002b/0x0041), not the canon command set.
  - Player-record delivery gap (live: player enters as ?©Ï†ú placeholder, stats 0): the created character is not delivered as the authoritative player record by default ??LOGH_WORLD_PLAYER / LOGH_POSTLOAD_RICH_CHARACTER / LOGH_SEED_CANON_NPCS all default OFF, so the HUD falls back to ?©Ï†ú. The 0x0356 seat path that would bind the player to the unit list is likewise gated off by default.
  - Live observation: even WITH the postload flags on, command.rowCountD4=24 is NONZERO but originates from the client's own resident card master (DAT_007ccffc+0x3416d8 via FUN_004c8700), not from server 0x0305 ??so 0x0305 is not the gating data for rowCount in that path. The actual C002 wall is downstream UI admission (selection controller +5 closed, invalid tab1 close via FUN_004f6680), documented in docs/logh7-c002-admission-discriminator-2026-06-28.md:335-351.
- contentGaps:
  - No card-master table mapping card_id -> factory-id list in the client wire layout (record+0x14 command_count u8, record+0x16 factory ids u16 LE; rendered widget id = factory+0x43 per info-records.mjs:102). strategy-commands.json/cards-cp-orgs.json describe commands and grant GROUPS (?ã‰∫∫?≥„Éû?≥„ÉâÁæ?‰ΩúÊà¶?≥„Éû?≥„ÉâÁæ? in prose but the concrete per-card factory-id numbering is NOT recovered.
  - Only two hardcoded probe factory ids exist: 0x002b, 0x0041 (login-session.mjs:523) ??P3 guesses, not canon. The ?ã‰∫∫/??ï∑ always-held cards (cards-cp-orgs.json:22-26) are not mapped to their factory id sets. Reconstructing this mapping would be P2/P3 ??do NOT promote as P0 and do NOT fabricate.
  - No card_id assignment per character/post: canon-initial-cards.json holds WHO holds which ?∑ÂãôÊ®©Èôê?´„Éº??(post names) but not the numeric card_id index the 0x0305 record uses (card_id/index@+0x00).
- rec: Two distinct issues; neither requires fabricating canon. (1) The 0x0305/0x0307 builders are byte-correct (RE-confirmed by FUN_004f5cb0: record+0x14 count, +0x16 factory ids LE; tests at logh7-login-session.test.mjs:2847-2890 pass) but the default world-walk zero-fills them. Since the canon factory-id numbering is NOT recovered (contentGap), do NOT ship the P3 probe ids (0x002b/0x0041) as if canon. Keep 0x0305 honest-empty by default and ONLY emit populated cards once a card_id->factory-id mapping is recovered (tag P2/P3 if reconstructed). (2) Per the live discriminator (docs/logh7-c002-admission-discriminator-2026-06-28.md), the C002 command-menu wall is currently NOT the 0x0305 card data (client shows rowCountD4=24 from its own resident master) ??it is client-side UI admission (selection controller +5 / tab1 close). So the highest-value, evidence-justified server fix is to deliver the PLAYER as the authoritative record by default-on in the live profile (LOGH_WORLD_PLAYER + LOGH_POSTLOAD_RICH_CHARACTER + LOGH_POSTLOAD_ACTION_LIST_SEATS + LOGH_SEED_CANON_NPCS) so the player is not a ?©Ï†ú placeholder with stats 0 and the 0x0356 seat enters slot 0x67 ??these levers already exist and are byte-verified; they just default OFF. Continue the client-side admission RE (FUN_004f6680 valid-tab) before claiming the command menu works live.

## institutions-facilities-arms (static reference catalogs: institution 0x0320/0x0321, static arms 0x0310/0x0311, static fighters 0x030e/0x030f, static unit-troop 0x030c/0x030d, static unit-ship 0x030a/0x030b, plus card-command 0x0306/0x0307, power-distribution 0x0308/0x0309)
- status: **partial**
- gates: LOGH_CONTENT_DB default '1' (PLAYABLE_ENV_DEFAULTS logh7-config.mjs:73) -> ON: contentPack.institutions/rooms populated from DB-backed source (buildContentPackDataFromSource->loadInferredCatalogs, content-adapter.mjs:227-228,461; auth-server.mjs:1299-1301). If '0', CANON_CONTENT used (auth-server.mjs:1304) which lacks institutions/rooms -> seeds fall back to the single {id:1,nameCatalogId:1} stub (inferred-content.mjs:259-260). | LOGH_WORLD_PLAYER default '1' (logh7-config.mjs:49) -> ON: gates the 0x0f02 world-load institution push (login-session.mjs:1664,1680). The on-demand 0x0320->0x0321 handler is NOT gated by this. | LOGH_WORLD_IMPORT_BASES default unset -> worldImportBaseRecordsEnabled()= (env !== '0') = ON (login-session.mjs:209). Gates worldImportBaseSourceInners (institution + base records). | LOGH_PLANET_BASE_RECORDS default '1' (logh7-config.mjs:60) -> ON: surfaces planet/fortress bases so institution seeds get per-base ids (login-session.mjs:1372,1479). | LOGH_STATIC_SHIPS default UNSET (NOT in PLAYABLE_ENV_DEFAULTS) -> staticShips OFF (config.mjs:175). With it OFF the 0x030a->0x030b handler does NOT fire; 0x030b falls to the zero-fill walker (count=0 ship master). | ARMS/FIGHTERS/TROOP/POWER-DIST/CARD-COMMAND: NO gate flag exists -- there is simply no branch, so they always hit the zero-fill walker regardless of env.
- deliveryGaps:
  - 0x0311 Arms: server HAS a byte-exact builder but NEVER sends populated data -- no REQ 0x0310 branch, so the 0x0311 panel is painted EMPTY by the zero-fill walker (login-session.mjs:2418). DELIVERY GAP (data exists in code, just nothing to seed).
  - 0x030f Fighters: builder exists, never wired -> zero-fill (count=0). DELIVERY GAP.
  - 0x030d Unit-Troop: builder exists, never wired -> zero-fill. DELIVERY GAP.
  - 0x0309 Power-Distribution: builder exists, never wired -> zero-fill blob. DELIVERY GAP.
  - 0x0307 Card-Command: builder exists and is wired ONLY behind the LOGH_COMMAND_TABLE_PRELOAD_PROBE probe (login-session.mjs:537); the normal REQ 0x0306 path falls to zero-fill. DELIVERY GAP by default.
  - 0x030b Unit-Ship: server HAS the full 63/64-ship master (ship-stats.json + DB) AND the builder, but it is GATED OFF by default (LOGH_STATIC_SHIPS unset). Real ship master is NOT delivered by default -> zero-fill ship spec sheet. DELIVERY GAP (gated-off).
  - 0x0321 Institution spot resolution: the seed maps only element id + first-room spotKey to field04 (inferred-content.mjs:267-274); institution/spot scalars are otherwise catalog-id placeholders (P2). Live defect: Î∞úÌï†??base panel shows '?§Ìèø Î∂àÎ™Ö' = the spot lookup (client matches spot.field04 against PLAYER_INFO +0x40/+0x44, copied from char source +0x20) does not resolve because the player character is delivered as the ?©Ï†ú placeholder (stats 0) -- the created character is NOT seeded as the active player record, so PLAYER_INFO +0x40/+0x44 carries the wrong/zero spot key and no spot.field04 matches. The institution record is structurally delivered but its spot data is unpaintable against a bad player anchor.
  - Cross-domain consequence: because the player-character delivery gap (created char not delivered as player record -> stats 0, ?©Ï†ú placeholder) leaves the spot key wrong, even the structurally-correct 0x0321 facilities/spots cannot resolve -> the base/planet panel degrades to '?§Ìèø Î∂àÎ™Ö'.
- contentGaps:
  - No real ARMS table (0x0311): the client expects arms[27][8] u16 hit/spread values (info-records-static.mjs:400-410, _INF:ResponseStaticInformationArms @0x760c64). No P0/P1/P2 content file holds these; would need recovery from the original static dump or RE of FUN_00412f90. Do NOT fabricate.
  - No real FIGHTERS table (0x030f): up to 4 fighter/spartanian rows {airbattle,antiship,defence,cruising} (info-records-static.mjs:373-388). No content source.
  - No real UNIT-TROOP table (0x030d): up to 16 ground-troop classes (info-records-static.mjs:340-360). No content source (DB unit_types=38 is a different unit catalog, not the 0x30d ground-troop master; mapping unproven).
  - No real POWER-DISTRIBUTION blob (0x0309): the global recharge/fill curves (move/warp/sensor/shield/beam/gun, info-records-static.mjs:217-255). No content source.
  - No real CARD-COMMAND grant table (0x0307): per-card command descriptors (info-records-static.mjs:174). No content source for the normal world-load path.
  - PHYSICAL FACILITIES content gap for 0x0321: all-names.json institutions are ORG-POST names (P2), not the ?≤Ë°õ/?†ÂÖµ/ÂØæÁ©∫/Ë°õÊòü base-facility records the client panel expects (institution-record.mjs header). Real per-base facility + spot[] data does not exist in any content file. The institution/spot scalar field names remain PROVISIONAL (field00/field04/field08).
  - SHIP-MASTER stat semantics gap (0x030b): the post-w06 u16 stat slots are MEDIUM-confidence (info-records-static.mjs:306); even when LOGH_STATIC_SHIPS=1, several stat meanings are reconstructed (P2), not original.
- rec: Two minimal, evidence-justified fixes that deliver REAL data without fabrication: (1) Wire the existing static builders for the normal request path so the panels stop painting empty: add REQ-code branches (mirroring the 0x030a/0x030b pattern at login-session.mjs:2406) for 0x030c->0x030d (UnitTroop), 0x030e->0x030f (Fighters), 0x0310->0x0311 (Arms), 0x0308->0x0309 (PowerDist), 0x0306->0x0307 (CardCommand) calling the matching build*Inner from createInfoRecordsStaticState. BUT only emit non-zero counts where real P0/P1 content exists -- for Arms/Fighters/Troop/PowerDist no content exists, so leaving them at count=0 is CORRECT (do not fabricate hit tables); the actionable item is to recover those static tables from the original client static dump / RE (FUN_00412f90 arms, FUN_00412d70 fighters, FUN_00412770 troop, FUN_00410690 power) before populating. (2) For ship master, flip LOGH_STATIC_SHIPS into PLAYABLE_ENV_DEFAULTS (config.mjs:43) ONLY after a live A/B (the file warns the client previously advanced past an empty 0x30b; the safe cap LIVE_SAFE_STATIC_SHIP_CAP exists at login-session.mjs:180) -- this delivers the real 63-ship manual-derived master. (3) ROOT-CAUSE the '?§Ìèø Î∂àÎ™Ö' defect outside this domain: ensure the created/lottery character is seeded as the active player record so PLAYER_INFO +0x40/+0x44 (spot key) is correct; only then will the already-correct 0x0321 spot.field04 entries resolve. Respect planetless-system rule (journals #53/#55): do not invent planet/facility rows for legitimately empty systems. Keep institution org-post names tagged P2 and do not promote to original.

## lobby-session-account: session catalog (0x2005->0x2006), lobby character cards (0x2003->0x2004), account roster-priming (0x1000/0x1002/0x1004 -> 0x1001/0x1003/0x1005), session select (0x2009->0x200a), announce (0x2003 SysSessionAnnounceNotify)
- status: **partial**
- gates: LOGH_SESSION_DB (logh7-server.mjs:150-165): DEFAULT = DEFAULT_SESSION_SQLITE_PATH (lobby-sessions.sqlite) -> persisted 2-session catalog loaded by default; 0/off/memory -> in-memory DEFAULT_SESSION_RECORDS | LOGH_ACCOUNT_DB (logh7-server.mjs:851): DEFAULT unset -> no account/character persistence; created chars not persisted. Live launch (logh7_launch_config.py:63) only passes it through, does not set it | LOGH_ACCEPT_ANY_GIN7: serve-auth default off, but live launch SETS =1 (logh7_launch_config.py:38) -> accept-any login, no registry -> no persistence | LOGH_LOBBY_RICH_CHARACTERS / LOGH_WORLD_CHAR_ID (auth-server.mjs:110,114): DEFAULT unset -> lobby cards use minimal placeholder | LOGH_WORLD_PLAYER (===1, default OFF; login-session.mjs:197) -> live SETS =1 (:45) | LOGH_SEED_CANON_NPCS (===1, default OFF; :201) -> live SETS =1 (:51); stamps top NPC as ?©Ï†ú | LOGH_PLANET_BASE_RECORDS (!==0, default ON; :1372) -> live SETS =1 (:50) | LOGH_BASE_ECONOMY (!==0, default ON; :218) | LOGH_PROVISIONAL_BASE_PARAMETER_NOTIFY (===1, default OFF; :221) -> 0x0337 NotifyBaseParameter (planet population/food/security panel); NOT set by live launch config -> planet economy panel never painted | LOGH_CONTENT_DB (config default '1', logh7-config.mjs:73) -> useDb TRUE -> contentPack from logh7-content.db (85 systems/281 planets; 5 systems have NULL canon cells), not thinner CANON_CONTENT P3 seed | LOGH_POSTLOAD_PLAYER_RECORD / LOGH_POSTLOAD_RICH_CHARACTER (===1, default OFF; :303,300) -> live SETS =1 (:46-47)
- deliveryGaps:
  - CREATED CHARACTER NOT DELIVERED AS WORLD PLAYER RECORD (live: player enters as ?©Ï†ú placeholder, stats 0): 0x1008 registers the character into the LOBBY session lobbyCharacters (login-session.mjs:2535) but world entry runs on a SEPARATE conn3 session where generatedCharacterId=0, re-seeded ONLY from account-profile persistence (loadAccountProfileCharacters :816-844). With no LOGH_ACCOUNT_DB (default; live uses LOGH_ACCEPT_ANY_GIN7=1, no registry) getProfileCharacters=[] -> hasRealPlayerChar() FALSE (:1068-1072) -> activeCharacterId() falls back to worldCharId()=1 -> playerRecord serves canon char 1 (sovereign) HUD-stamped ?©Ï†ú, or synthFallbackPlayer ?†ÏûÑ ?¨Í? with P3 abilities (:1073-1104). Real created character + abilities dropped.
  - PLANET/BASE ECONOMY PANEL EMPTY for current spot (live: Î∞úÌï†??shows ?§Ìèø Î∂àÎ™Ö): 0x0337 NotifyBaseParameter (population/food/security/ideology/religion/support) built by baseParamForSpot only when baseEconomyEnabled() AND baseParameterNotifyEnabled() (login-session.mjs:1516); LOGH_PROVISIONAL_BASE_PARAMETER_NOTIFY default OFF and NOT set by live launch config -> panel never gets planet params even though logh7-content.db + planet-economy.json hold Valhalla (?¥„Ç°?´„Éè??id70/empire) 4 planets incl Odin (?™„Éº?á„Ç£?? food 13312). Delivery gap, not content gap.
  - SESSION CATALOG POWERS shipped as zeros: 0x2006 carries powers d0/d1/d2=0 + empty world (sessionRegistry serves stub DEFAULT_SESSION_RECORDS) -> picker rows show names but no force/score summary
  - ANNOUNCE 0x2003 silent by default: SysSessionAnnounceNotify only sends with LOGH_LOBBY_ANNOUNCE_TEXT/_CP949_HEX or --announce or admin noticeState.set; resolveLobbyAnnouncementText returns null by default (auth-server.mjs:418-428), live launch sets none -> ?úÎ≤Ñ Í≥µÏ? panel empty
  - ACCOUNT roster size 0x1001 reports ownedCharacterCount from empty/placeholder lobbyCharacters for a fresh account -> account panel shows 0 owned even after a create that persists nowhere
- contentGaps:
  - Lobby session payload economy/score fields (powers[].d0/d1/d2) all 0 and world_json={} -> no original lobby session-summary data exists; only canon battle NAMES are real (P3 seed). No P0/P1 source for the lobby session row payload found in content/
  - Player-created characters have no canon ability BASE from the creation form (client widget defaults are never sent to server) -> content/roster/ability-seed.json is a P3 house-rule seed, not original (login-session.mjs:2490-2495); content limitation acknowledged in code, must not be promoted as original
- rec: No fabrication needed; fixes are delivery/wiring since character+planet content already exists at P0/P1. (1) Persist accounts/characters by default in the live path: wire LOGH_ACCOUNT_DB (logh7-account-registry.mjs already implements accounts.characters_json) into logh7_launch_config.py DEFAULT_FLAGS so a created character survives the lobby->conn3 handoff and is served as the authoritative player 0x0323 record via loadAccountProfileCharacters; this makes hasRealPlayerChar() true and removes the ?©Ï†ú/stats-0 fallback. (2) Set LOGH_PROVISIONAL_BASE_PARAMETER_NOTIFY=1 (keep LOGH_BASE_ECONOMY default ON) in the live launch so 0x0337 paints the current-spot planet panel from existing P1 planet-economy content (Valhalla/Odin), fixing ?§Ìèø Î∂àÎ™Ö; respect planetless-system rule (baseParamForSpot already returns null when a system legitimately has no planets, journals #53/#55). (3) Treat lobby session powers/world (d0/d1/d2, world_json) as a known P3 stub; enrich only if a P0/P1 lobby session-summary source is recovered, do not invent force/score numbers. (4) Optionally provide an announce string via admin noticeState or --announce for the ?úÎ≤Ñ Í≥µÏ? panel (operator content, not original-data).

## chat-notify-misc (chat 0x0f1c/0x0f1d/0x0f1e, NotifyMovedGrid 0x0b07 + combat 0x042x, simple-info-end 0x1201, text-info/mail 0x0f08/0x0f09, player-id 0x0204)
- status: **partial**
- gates: LOGH_RELAY default '1' (PLAYABLE_ENV_DEFAULTS, logh7-config.mjs:68) ??chat/notify relay ON, but only broadcasts to OTHER in-world connections | LOGH_AUTHORITATIVE default '1' (logh7-config.mjs:69) ??enables processCommand authoritative chat/move path | LOGH_WORLD_PLAYER default '1' (logh7-config.mjs:49) + LOGH_POSTLOAD_PLAYER_RECORD default '1' (logh7-config.mjs:50) + LOGH_POSTLOAD_RICH_CHARACTER default '1' (logh7-config.mjs:51) ??push 0x0204/0x0325/0x0323 player record on world entry | LOGH_POSTLOAD_SIMPLE_INFO default OFF (NOT in PLAYABLE_ENV_DEFAULTS; read at logh7-login-session.mjs:301) ??so the 0x1200..0x1201 simple-info transaction is NOT sent by default | LOGH_NPC_AI default OFF + LOGH_NPC_SEED default OFF (logh7-login-session.mjs, auth-server.mjs:1369) ??no AI-driven combat 0x042x notifies by default; gated also on worldRelay.size()>0 | LOGH_FLEET_MOVE_PROBE default OFF (logh7-login-session.mjs:285) ??server-pushed 0x0b07 demo OFF; 0x0b07 otherwise only on inbound command | LOGH_RELAY_TEST default OFF (auth-server.mjs:2085) ??self-test 0x0f1c chat OFF | LOGH_BATTLE_ENTRY_PROBE / LOGH_STATE_TRANSITION_PROBE default OFF ??0x042f tactical-mode notify not pushed by default
- deliveryGaps:
  - ENDIANNESS BUG (Korean chat roundtrip): GridChat 0x0f1c inbound parseInboundChat reads time as readUInt32BE and message chars as readUInt16BE (logh7-command-engine.mjs:259,266), but the client serializes UTF-16LE (client reader FUN_004be660/FUN_004be6f0 raw LE dword memcpy, redex-confirmed) and the receive-form builder buildCommandGridChatInner writes writeUInt16LE (logh7-login-protocol.mjs:1532-1537). The SpotChat sibling correctly uses LE (logh7-social.mjs:98,127). So a real Korean GridChat message is byte-swapped into mojibake before rebroadcast. The command-engine test passes only because its fixture makeInboundChat also uses BE (tests/server/logh7-command-engine.test.mjs:68) ??a self-consistent but client-mismatched fixture.
  - Chat target='others' (logh7-command-engine.mjs:399) + relay broadcast excludes sender (logh7-world-relay.mjs:105) means a SOLO player never sees their own chat echoed; with the live single-player session there is zero chat delivery. SpotChat/combat have the same single-connection no-recipient property.
  - simple-info commit 0x1201 not delivered by default: buildSimpleInfoTransaction is gated behind LOGH_POSTLOAD_SIMPLE_INFO (default OFF, logh7-login-session.mjs:1950) ??the 0x1200..0x120f..0x1201 state-sync pump never fires on a default boot, so any panel relying on the simple-info delta buffers stays empty.
  - Combat 0x042x notifies are never produced on a default solo boot: they require either an inbound tactical command from a second client or the NPC AI loop (LOGH_NPC_AI/LOGH_NPC_SEED, both default OFF). No AI-vs-AI activity ??no combat notifies.
  - Player-record stats-0 / ?©Ï†ú placeholder (live defect): when no real created/charged/account/explicit char exists, activeCharacterId falls back to worldCharId=1 (sovereign) and playerRecord substitutes synthFallbackPlayer (logh7-login-session.mjs:1073-1104). The genuine create path DOES stamp real abilities + stamina=STAMINA_FULL into 0x0323 (logh7-login-session.mjs:2500-2533), so the live 'stats 0' symptom is the created character not being delivered as the player record (the session resolved a placeholder, not the created char) ??a delivery/wiring gap in the player-id/character-record lane, not a content gap.
  - Mail begin/status builders 0x0f08/0x0f09 exist but are dead: never called on any login/world path (only in tests), and 0x0f08 is in DIAGNOSTIC_INNER_PAYLOAD_CODES only (auth-server.mjs:382) ??the mail-transfer sequence is never initiated.
- contentGaps:
  - No mail/messenger content exists: 0x0f05 address book, 0x0f07 presence roster, 0x0f08/0x0f09 mail transfer are zero-fill stubs (logh7-account.mjs:231-303) ??the social inbox/contact panels have no data to paint (acceptable: empty inbox is honest, P0 layout unknown beyond count anchor).
  - No persisted chat log/history content ??chat is purely live relay; a player joining mid-session sees no backscroll (by design; not a content file gap).
  - Per-record FIELD semantics of 0x12xx simple-info deltas are unknown beyond first-dword id (codec/simple-info.mjs:36-38, LOW confidence) ??server cannot populate meaningful character/base deltas even if the transaction were enabled.
- rec: Two evidence-justified fixes, no fabrication. (1) Fix the GridChat 0x0f1c endianness to match the client and its own SpotChat sibling: in logh7-command-engine.mjs:259,266 change readUInt32BE/readUInt16BE to LE (readUInt32LE/readUInt16LE), and update the fixture tests/server/logh7-command-engine.test.mjs:68 to writeUInt16LE ??RE-confirmed via FUN_004be6f0 raw-LE memcpy and the LE receive-form builder. This restores Korean (cp949-typed ??UTF-16LE wire) chat roundtrip. (2) For the live solo player-record defect, ensure the freshly-created character is what activeCharacterId()/playerRecord() resolve so the real 0x0323 (with abilities + stamina=STAMINA_FULL, logh7-login-session.mjs:2500-2533) is delivered instead of the worldCharId=1 synthFallbackPlayer; this is a session-state wiring fix in the player-id lane, not a content change, and keeps the no-auto-emperor clamp intact. Leave mail/messenger zero-filled (no content; do not invent), and consider adding LOGH_POSTLOAD_SIMPLE_INFO to PLAYABLE_ENV_DEFAULTS only after the 0x12xx field map is RE-confirmed (currently LOW confidence ??enabling it now would push only id+zero deltas).


## 2026-06-29 Live Correction

Standard playable/live profile no longer enables the whole static/P3 bundle. Keep `LOGH_STATIC_SHIPS=1` because `bisect-ships-20260629` reached world view through `0x0f02`, `0x0f03`, `0x0f06`, and `0x0356`. Keep `LOGH_STATIC_TROOPS=0`, `LOGH_STATIC_FIGHTERS=0`, `LOGH_STATIC_ARMS=0`, `LOGH_STATIC_POWER_DISTRIBUTION=0`, and `LOGH_STATIC_MASTER_PLAYABLE_SEED=0` by default. `cmdpatch-smoke-20260629` showed `ships+troops` exits after `0x0f01`; the full P3 seed bundle `bisect2-bundle-20260629` and repeated `seed+ships` runs do the same. `seed+ships` survives only with `LOGH_STATIC_SHIPS_LIMIT=1`, so troop/P3 seed tables remain diagnostic/dev-only until the ship-row interaction is root-caused.

# ===== SYNTHESIS =====

Task complete.
