<!-- Generated: 2026-06-26 | C002 deep-RE B조 (패널 빌더 + 씬셋업 + unit-list) -->

# C002 deep-RE B조 — 전략명령 패널 빌더 / 씬 KIND FSM / unit-list

읽기전용 정적 RE. 바이너리 소비자 = `G7MTClient.exe` (Ghidra 인덱스 `.omo/ghidra/export/G7MTClient/`).
도구 = `RE/tools/logh7_redex.py` (cwd 리셋 때문에 `--export E:/logh7-revival/.omo/ghidra/export/G7MTClient` 절대경로 필요).
모든 함수/오프셋 = **P0 클라 바이너리 확정** (디컴파일 C 기준). 데이터값(unit count 등)은 런타임 의존이라 P0 승격 안 함.

## 호출 그래프 (정적)
```
FUN_004b68f0 (mode 디스패처)
  └─ iVar7==1 / iVar7==2 분기 + param_1[0x126711]/[0x126718] mode 바이트 게이트
       └─ FUN_0054e570 (씬 KIND FSM)  ── *param_1=KIND ──┐
            ├ KIND==1 → FUN_005123b0                      │
            ├ KIND==2 → FUN_004ff3c0 (★전략명령 패널 씬셋업)
            └ KIND==3 → FUN_0051ca30
                 FUN_004ff3c0 → FUN_004f6680 → ... → FUN_004fc4e0
                                                        └─ FUN_004f6040 (★상류근본 패널 빌더)
                                                             └─ FUN_004fe890(...,0x67,...) = 0x67 unit-list 위젯 생성
```

## 함수별 분석

### FUN_004f6040 — ★상류근본 0x67 unit-list 패널 빌더  (conf: HIGH)
- 호출규약: `bool __thiscall FUN_004f6040(int *param_1, int param_2)`. param_1=ecx=패널 객체 this, param_2=소스 컨텍스트(전략 클라/뷰).
- **param_2 != 0 일 때만** 위젯 트리를 빌드(param_2==0 = 파괴/리셋 경로). 즉 빌더 실행의 1차 게이트 = param_2(소스 컨텍스트) 비-NULL.
- 동작: 로컬 위젯 디스크립터 배열(local_4fc/local_420/local_90 등 36워드 템플릿)을 채운 뒤
  `iVar2 = FUN_004fe890(param_2, 0x67, local_90, 1, param_1+1, 0)` 로 **타입 0x67 위젯**을 생성해 `*param_1`에 저장.
  이어서 FUN_004fe930로 자식 위젯(리스트 행/스크롤) 생성, `param_1+0x12`(local_504) 등 서브-위젯 슬롯에 핸들 적재.
- DAT/오프셋: `&DAT_007791a0`(라벨/스타일 리소스), 색상 리터럴 0x3f800000/0xff000000/0xff002040; this 멤버 param_1+1(위젯루트), +0x12, +0x10(local_504[0x10]) 자식 핸들.
- 반환: bool(빌드 성공). 피호출자: FUN_004f65c0, FUN_004fe890, FUN_004fe930, FUN_005923a0(에러).
- **자연 트리거 조건**: (a) 씬 KIND==2 경로(FUN_004ff3c0)가 활성이고 (b) 호출자 FUN_004fc4e0가 `FUN_004f6040(param_2)`를 param_2≠0로 호출해야 함. autologin 월드에서 미실행 = KIND==2 씬이 활성화되지 않아 FUN_004fc4e0 자체가 안 돎(상류 mode 게이트 미충족).

### FUN_0054e570 — 씬 KIND FSM (conf: HIGH)
- 호출규약: `void __thiscall FUN_0054e570(int *param_1, int param_2)`. param_1=씬 매니저 this, param_2=새 KIND 값.
- 동작: 기존 씬 teardown(FUN_0050c240/FUN_0054e6c0/FUN_0050ba80) → 새 씬 객체 할당(FUN_0050b8e0) param_1[3]에 저장 → `*param_1 = param_2`(KIND 확정) → FUN_0054e760(param_2)로 위젯 디스크립터 시드 → **KIND 분기**: 1→FUN_005123b0, **2→FUN_004ff3c0**, 3→FUN_0051ca30, else 에러 DAT_0076f1cc.
- DAT: `&DAT_00788d8c`(param_1[3]==0 가드 에러), `&DAT_00bd5a18`(팩토리 키), `&DAT_0076f1cc`(KIND 미지원 에러); param_1[3]+0x3a0=1(씬 ready 플래그), param_1[1]=0.
- 반환: void. 호출자: **FUN_004b68f0**(mode 디스패처, 3곳). 피호출자: FUN_0054e760, FUN_004ff3c0 등 KIND 핸들러.
- **핵심**: 어떤 KIND로 들어오느냐가 모든 것을 결정. param_2(KIND)는 FUN_004b68f0의 `iVar7==1/==2` 분기와 mode 바이트(0x126711/0x126718)에 의해 선택됨 → C002 mode-배타 근본과 직결.

### FUN_004ff3c0 — KIND==2 전략명령 패널 씬셋업 (conf: MED-HIGH)
- 호출규약: `void __fastcall FUN_004ff3c0(int param_1)`. param_1=KIND==2로 새로 만든 씬 객체.
- 동작: 패널 씬 초기화 진입점. (디컴파일 단편) 내부에서 FUN_0050cf40 게이트 확인 후 FUN_004f6680 → 빌더 체인(FUN_004fc4e0→FUN_004f6040)으로 진행, unit count `*(byte*)(src+0x270)` 기반 리스트 채움.
- 반환: void(undefined 1 wrapper). 호출자: FUN_0054e570(KIND==2 분기). 피호출자: FUN_004f6680, FUN_0050cf40.
- 미실행 사유: KIND==2 씬이 안 켜지면 이 함수 자체가 호출 안 됨.

### FUN_004f68f0 — unit-list 채우기 루프 (conf: HIGH)
- 호출규약: `undefined4 __thiscall FUN_004f68f0(int *param_1, undefined *param_2)`. param_1=패널 this, param_2=유닛 컬렉션 소스.
- 동작: `uVar4 = *(byte*)(param_2+0x270)` = **유닛 개수**. param_1[0x188]=uVar4 저장, FUN_004f6680 호출, uVar4>0이면 `local_20 = param_1 + uVar4 + 1`부터 역방향 루프: 각 유닛마다 FUN_004c8700/FUN_004c8cb0/FUN_004c8c40(분류)→
  iVar2==0(미할당)/==1(인물커맨더 FUN_004c9340/FUN_004c9010)/==2 별 라벨 포맷(FUN_00646616) → FUN_005034d0(iVar1)로 행 위젯 적재.
- DAT/리터럴: `s__s____s_00779198`, `s___s___s_00779190`, `&DAT_00771e08`(빈 라벨), `PTR_DAT_007b3608`; 멤버 +0x187/+0x188/+0x189/+0x18a, 유닛 오프셋 +0x270.
- 반환: undefined4. 호출자: FUN_004fc4a0. **이것이 0x67 패널의 실제 행 데이터를 0x270 unit count에서 읽어 채우는 곳** — count=0이면 빈 리스트.

### FUN_004f6600 — 패널/리스트 보조(슬롯 클리어/리셋) (conf: MED)
- 호출규약: `__thiscall FUN_004f6600(int *param_1, ...)`. param_1=패널 this.
- 동작: 멤버 블록 제로화 루프(`for(iVar1=0x145; ...) *puVar2=0`), param_1[0x187]=0/[0x188]=0/[0x189]=0xffffffff/[0x18a]=0 = unit-list 상태 초기화/리셋.
- 반환: void. 호출자: FUN_004fd100. 빌드 전 리스트 슬롯 클리어 역할.

## ★ FUN_004f6040 실행 조건 / autologin 미실행 원인 (정적 증거)
1. FUN_004f6040(빌더)는 **param_2≠0** + 호출자 FUN_004fc4e0가 도는 것이 전제.
2. FUN_004fc4e0는 KIND==2 패널 씬셋업(FUN_004ff3c0→FUN_004f6680) 경로에서만 진입.
3. KIND==2는 FUN_0054e570(씬 FSM)에 param_2(KIND)=2로 들어와야 설정됨.
4. FUN_0054e570 호출은 **FUN_004b68f0(mode 디스패처)** 안의 `iVar7==1 / iVar7==2` 분기에서만 발생하고, 주변이 mode 바이트 `param_1[0x126711]` / `param_1[0x126718]`로 게이트됨(메모리 기록 C002 mode-배타 게이트와 동일 바이트).
→ **autologin 월드에서 미실행 근본 = mode 디스패처(FUN_004b68f0)가 전략 패널 mode(iVar7==2 / 0x126718 세트)로 전이하지 못해 씬 KIND==2가 안 켜지고, 따라서 FUN_004fc4e0→FUN_004f6040(0x67 빌더)이 호출되지 않음.** 입력/마우스 문제 아님(메모리 라이브 반증과 정합) — 씬 KIND 전이 게이트 문제.

## 불확실성 / 다음 검증 후보
- FUN_004b68f0의 iVar7 산출식과 0x126711/0x126718 세팅 트리거(어떤 메시지/클릭이 mode2로 전이시키는지) = 다음 RE 타겟(B조 범위 밖, A/C조).
- FUN_004fc4e0가 FUN_004f6040에 넘기는 param_2(소스 컨텍스트) 비-NULL 조건의 런타임 출처 미확정.
- unit count `*(byte*)(src+0x270)`의 src가 서버 전략 데이터(0x0313/0x0315)인지 클라 로컬 셀렉션인지 = 라이브 probe로 0x270 읽기 필요(읽기전용).

## 산출 명령 (재현)
```
EXP=E:/logh7-revival/.omo/ghidra/export/G7MTClient
python RE/tools/logh7_redex.py --export $EXP func 0x004f6040
python RE/tools/logh7_redex.py --export $EXP calls 0x004f6040   # → FUN_004fc4e0
python RE/tools/logh7_redex.py --export $EXP calls 0x004ff3c0   # → FUN_0054e570
python RE/tools/logh7_redex.py --export $EXP calls 0x0054e570   # → FUN_004b68f0
```
