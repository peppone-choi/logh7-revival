# LOGH VII 서버 적대적 감사 (2026-06-20) — 검증된 갭 41개

8 도메인 병렬 감사 + 각 발견 독립 적대검증(51 에이전트). isReal=true 41 / 기각 2. 중복(경제-정복 동기화 3중 보고 등) 통합. 상태: ☐ 미착수 / ☑ 닫힘.

## P0
- ☐ **private-delivery 누수**(command-social): `dispatchNotifies`(auth-server:754-768)가 `targetConnectionId` 무시 → 귓속말/개인메일/IM/명령메일이 같은 월드 전원 브로드캐스트(프라이버시+캐논 위반). Fix: targetConnectionId 있으면 worldRelay.send 한 곳만.

## P1
- ☐ **canCommand 미배선**(combat): 저사기/혼란 유닛 지휘불가(p442) 게이트가 죽은 export. command-engine MOVE/ATTACK/SHOOT/FIGHT/CHANGE_MODE + battle-ops maneuver에 게이트 추가. (ship에 confusion 필요)
- ☐ **economy-정복 소유 desync**(strategic/economy 3중): conquerSystem이 economyState 행성 faction 미갱신 → 세금이 상실 진영에 적립. economy.setPlanetOwner + runStrategicTickOnce에서 conquests 소비. (opt-in LOGH_ECONOMY+LOGH_STRAT_SIM)
- ☐ **age-drift boot no-op**(strategic): boot가 abilities+age 캐릭터 0명 등록 → 월간 훅 scanned=0. 캐논 시나리오/seedStrategicFleets 사령관에 abilities+age upsertCharacter.
- ☐ **merit 리셋 누락**(personnel 캐논§5.6): RANK_UP→achievement 0, RANK_DOWN→100.
- ☐ **SpotChat 0x0f1d 가로채기**(command-social logic): command-engine GridChat 핸들러가 0x0f1d를 잘못 파싱. 핸들러를 0x0f1c 전용으로, 0x0f1d→processSocial.
- ☐ **social charId=connectionId**(command-social): addPlayer charId=connectionId → connectionOfChar 실패. 실 char id 전달.
- ☐ **economyState 스냅샷 누락**(persist): 재시작 시 국고 소실+이중적립. composeSnapshot에 economy 포함+restore.
- ☐ **evaluateEnding 전략틱 미호출**(strategic, medium): 정복 후 종료평가 안 됨(종료가 economy 클록에만). runStrategicTickOnce에서 conquests>0 시 evaluateEnding. (단 economy 기본 ON이라 전제 부분 오류)
- ☐ **정치/첩보가 支持率/治安 미변경**(economy): adjustApproval/adjustSecurity 호출처 0. (대부분 opcode-게이트 — 주석 정정 or NPC틱 배선)

## P2
- ☐ computePlanetTax approval undefined→NaN(economy:38). Number.isFinite 방어.
- ☐ 地上戦 반격피해 attackerStrengthAfter 공격측 미적용(command-engine:574).
- ☐ SPECIALLY_RANK_UP 정원캡 우회(personnel:357-397). canPromoteTo 게이트 추가.
- ☐ 자동진급(sortLadder #1/30일) 미배선.
- ☐ coup decisiveVictory 인과 반대(intel:42-45). 개시게이트서 제거→evaluateEnding noCoup 조건.
- ☐ coupLoyalty Map 캐릭터/유닛 키스페이스 혼용(intel:59). 분리.
- ☐ restore 별칭(aliasing) 변이(world-state:697-703). 복원 시 복제.
- ☐ entity-store 사문(dead code). 설계결정(흡수 or 채택) — defer.
- ☐ A2 codec 추출 부분완료(트랜스포트/login-protocol 미이동). 워크트리 리팩터 — defer.
- ☐ 메일120/주소록100/亡命 wipe/전투중정지/메신저 FSM 미구현(social).
- ☐ 작전계획 입안≠발령 파이프라인 미배선(operation-plan.mjs 고아).
- ☐ coup_conduct 생산자 0(라이브 항상 0).
- ☐ 諜報 3종(一斉捜索/侵入/帰還) 미구현(espionage).
- ☐ dispatchNotifies/social 통합 테스트 부재(P0 회귀방지).

## P3
- ☐ addTreasury 음수/NaN 무가드 / countAtRank null excludeId(id0 제외) / SPECIALLY_RANK_UP funder 소유권 미검증 / resolveLandCombat result=2 미반환 / 戦闘艇 intercept 모드 하드코딩 / 사령관 charId 진영충돌 / nextOrderId 미복원 / rngState 스냅샷 누락 / asInt 빈문자열→0 + asBool vs isEnabled 분기 / 탐지된 모의 계속 포섭.

## 기각(거짓양성)
- 戦闘艇/항복권고 통합테스트 부재 — 실제로 battle-ops.test.mjs:574+ 존재.
- info-records builders 미라우팅 — 실제로 login-session에 전부 배선(잘못된 파일 봄).

## 진행 — 닫힘 (2026-06-20 구현, ultracode)
- ☑ **P0 private-delivery 누수**: planNotifyDispatch(순수,export) — targetConnectionId면 unicast. +5테스트.
- ☑ **P1 SpotChat 0x0f1d 가로채기**: command-engine GridChat 핸들러 0x0f1c 전용화 → 0x0f1d→processSocial.
- ☑ **P1 canCommand 미배선**: MOVE/ATTACK/SHOOT/FIGHT에 저사기/혼란 지휘불가 게이트. +6테스트.
- ☑ **P1 merit 리셋**: 昇進→0/降等→100 (personnel). +2테스트.
- ☑ **P2 地上戦 반격피해**: 방어생존 시 attackerStrengthAfter 공격측 적용. +2테스트.
- ☑ **P2 抜擢 정원캡**: SPECIALLY_RANK_UP에 canPromoteTo. +1테스트.
- ☑ **P3 resolveLandCombat result=2**(방어승) / **P3 抜擢 펀더 소유권** / **P3 countAtRank null** / **P3 confusion 게이트**.

**잔여(후속)**: P1 economy-정복 동기·age-drift boot 시드·economyState 스냅샷·evaluateEnding 전략틱·政治
approval/security·social charId(딥 char모델); P2 computePlanetTax NaN·자동진급·coup decisiveVictory·coupLoyalty
키스페이스·restore 별칭·메일캡·작전파이프라인·coup_conduct 생산자·諜報 3종; P3 나머지.

## 진행 — 닫힘 (2026-06-20 Batch 4 경제)
- ☑ **P1 economy-정복 동기**: registerPlanet에 system 저장 + setSystemOwner/setPlanetOwner +
  runStrategicTickOnce가 result.economy 소비. economy 생성을 전략틱 앞으로 이동.
- ☑ **P1 economyState 스냅샷**: composeSnapshot entities.economy + boot 복원(시드 전).
- ☑ **P2 computePlanetTax approval NaN** (Number.isFinite). ☑ **P3 addTreasury 음수/NaN 가드**.
- +4테스트. 서버 **1027테스트**. (economy-정복 동기 중복 보고 3건 모두 이 픽스로 해소.)

**다음 잔여 우선순위**: P1 age-drift boot 시드(현 무유저 no-op)·evaluateEnding 전략틱·政治 approval/security
배선; P2 자동진급·coup decisiveVictory·coupLoyalty 키스페이스·restore 별칭·메일캡·작전파이프라인·coup_conduct
생산자·諜報 3종; P3 나머지(rngState/nextOrderId/asInt/탐지모의).

## 진행 — 닫힘 (2026-06-20 Batch 5 전략 종료)
- ☑ **P1 evaluateEnding 전략틱 미호출**: runStrategicTickOnce가 정복 시 evaluateEnding(minSystems:3) 호출 +
  over면 전략 스케줄러 정지. economy 클록 독립.
- ☑ **P2 종료 ≤3성계(캐논 §1.6)**: evaluateEnding minSystems 파라미터(기본0 보존, 전략틱=3).
- +3테스트. 서버 **1029테스트**.

### ⚠️ 보류 — age-drift boot 시드 (연령 데이터 부재)
P1 "age-drift 무유저 no-op"는 **연령 데이터 소스 부재**로 보류. character-roster.json: age/birth_year **0건**,
rank 희소(97중 다수 None)·문자열. applyMonthlyAgeDrift는 age 필요(age=0이면 스킵). 가짜 연령 조작은 캐논
의도 훼손이라 부적절. **해소 경로**: (1) 캐논 연령표/birth_year 데이터 복구(매뉴얼/IV-EX/위키), (2) 또는
age-drift를 연령 대신 다른 신호(전투경험/CP누적)로 재설계. 데이터 복구가 선행 과제. 현재 age-drift 배선
(world-state.applyMonthlyAgeDrift + strategic-sim 월간 훅)은 정상이며 age 데이터만 있으면 즉시 발화.

## 진행 — 닫힘 (2026-06-20 Batch 6 영속성 + Batch 7 coup/intel)
- ☑ **P2 restore/toSnapshot 별칭**: 양쪽 엔티티 복제(스칼라 얕은+중첩 deep). ☑ **P3 rngState 스냅샷**. +2테스트.
- ☑ **P2 coupLoyalty 키스페이스**: intel unitLoyalty Map 분리. ☑ **P3 탐지된 모의 포섭/설득 차단**. +2테스트.
- 서버 **1033테스트**.

**누적(P0+7P1+9P2+8P3 닫힘)**: social·combat·personnel·economy·strategic·persist·coup/intel.
**남은 큰 항목**: P1 age-drift(연령데이터 부재 보류)·政治 approval배선(opcode); P2 coup decisiveVictory 인과
(eval 배선 필요)·자동진급(sortLadder)·메일캡/라이프사이클·작전파이프라인·coup_conduct 생산자·諜報 3종·
entity-store 사문·A2 codec 추출; P3 nextOrderId·asInt·戦闘艇 intercept·commander charId.

## 진행 — 닫힘 (2026-06-20 Batch 8 諜報+config)
- ☑ **P2 諜報 3종**: 一斉捜索(massSearch/located)·侵入工作(intrusion)·帰還工作(returnOp) espionage 추가. +3테스트.
- ☑ **P3 config parseBool 통일**(asBool↔isEnabled 분기 제거) + **asInt 빈문자열 가드**. +3테스트.
- 서버 **1038테스트**. 누적: P0 + 7P1 + 11P2 + 10P3.

**남은 항목(대부분 大작업/배선의존)**: P1 age-drift(연령데이터 부재)·政治 approval(opcode); P2 coup
decisiveVictory 인과(eval 배선)·자동진급(sortLadder)·메일캡/라이프사이클·작전파이프라인·coup_conduct 생산자·
entity-store 사문·A2 codec 추출; P3 nextOrderId·戦闘艇 intercept(live RE 필요)·commander charId.

## 진행 — 닫힘 (2026-06-20 Batch 9 메일캡/charId)
- ☑ **P2 메일/주소록 캡 + 亡命 wipe**(캐논 §2.4): 메일함120·주소록100·clearContacts. +3테스트.
- ☑ **P3 commander charId 전역유일**(FACTION_WIRE 접두). +1테스트.
- 서버 **1042테스트**. 누적: P0 + 7P1 + 13P2 + 11P3.

**남은 항목(大작업/배선·데이터·RE 의존)**: P1 age-drift(연령데이터 부재)·政治 approval(opcode); P2 자동진급
(sortLadder #1 월간 — 배선가능, 다음 후보)·coup decisiveVictory 인과(eval 배선)·coup_conduct 생산자(intelState
world 배선)·작전파이프라인·메신저 FSM/전투중메일정지·entity-store 사문·A2 codec 추출; P3 nextOrderId(사문)·
戦闘艇 intercept(live RE).

## 진행 — 닫힘 (2026-06-20 Batch 10 자동진급)
- ☑ **P2 자동진급(sortLadder #1 월간)**: autoPromoteLadders(순수) + personnel.runMonthlyPromotions(功績=목표
  사다리 평균). sortLadder 죽은 export 해소. +3테스트. 서버 **1045테스트**.
- ⚠️ 월간 틱 배선: personnel state가 무유저 월간-틱 경로 + 로스터 시드가 있어야 발화(age-drift와 동일
  population 의존). 다음 배선 후보=캐논 로스터를 personnel/world 캐릭터로 boot 시드 → 월간 훅에서
  runMonthlyPromotions 호출(auto-promotion은 age 불필요라 시드만 되면 즉시 발화).

누적: **P0 + 7P1 + 14P2 + 11P3**. 남은 건 大작업/배선·데이터·RE 의존만:
- P1 age-drift(연령데이터)·政治 approval(opcode).
- P2 coup decisiveVictory 인과/coup_conduct 생산자(eval·intelState world 배선)·작전파이프라인(operation-plan 고아
  모듈 배선)·메신저 FSM/전투중메일정지·entity-store 사문·A2 codec 추출.
- P3 nextOrderId(사문)·戦闘艇 intercept(live RE).

## 남은 항목 — 착수 가이드 (2026-06-20 ultracode 세션 종료 시점)
바운드 갭은 전부 마감(서버 1045테스트). 아래는 **각각 무엇이 선행돼야 하는지** — 다음 세션이 바로 잡도록.

| 항목 | severity | 막는 것 | 착수법 |
|---|---|---|---|
| age-drift 무유저 발화 | P1 | **연령 데이터 부재**(roster age/birth_year 0건) | 캐논 연령표 복구(매뉴얼/IV-EX/위키)→scenario/roster에 age 주입. 배선·로직은 완비. |
| 政治 approval/security 배선 | P1 | **클라 opcode 미확정**(分列行進/武力鎮圧/演説 sub-action) | 라이브 캡처로 0x0900 sub-action 확정 후 economy.adjustApproval/Security 연결. |
| 자동진급/age-drift 월간 틱 실발화 | P2 | personnel state가 무유저 월간-틱 경로에 없음 + 로스터 미시드 | 캐논 로스터를 boot 시 world/personnel 캐릭터로 시드(achievement 포함)→strategic 월간 훅에서 runMonthlyPromotions 호출. **auto-promotion은 age 불필요라 시드만 되면 즉시 발화.** (캐릭터 모델 통일: world-state char에 achievement/title 추가 필요.) |
| coup decisiveVictory 인과 정정 | P2 | evaluateEnding 측 coup 배선 | canStartCoup에서 decisiveVictory 제거 + evaluateEnding에 activeCoups 조건(coup 있으면 完全勝利 박탈). 단 coupState가 world에 미인스턴스라 동시 처리. |
| coup_conduct 생산자 | P2 | intelState가 world에 미인스턴스 | createWorldState에 intelState 보관→NPC/전략틱이 addCoupLoyalty 누적→personnel record builder가 isCoupConduct 전달. |
| 작전계획 입안≠발령 파이프라인 | P2 | operation-plan.mjs 고아 + MakePlan sub-action 불확정 | strategy.mjs MakePlan이 operation-plan.validateOperationPlan/draft 사용하도록 배선 + 30일 타이머(라이브로 sub-action 확정 후 완성). |
| 메신저 1:1 FSM | P2 | **0x0f0e payload(open/accept/close+target) 불확정** | 라이브 캡처로 0x0f0e 레이아웃 확정 후 social에 per-conn {mode,peer} FSM. |
| entity-store 사문 | P2 | 설계 결정 필요 | world-state로 흡수 or entity-store 채택(단일 영속모델) 결정. |
| A2 codec 추출 | P2 | 大규모 기계적 리팩터 | 워크트리에서 transport codec + login-protocol 빌더를 src/server/codec/로 이동(re-export shim, 테스트 그린/이동). |
| 戦闘艇 intercept 모드 | P3 | **0x040e sub-action 판별자 라이브 미확정(LV-5)** | 라이브 캡처 후 battle-ops mode 동적선택. |
| nextOrderId 복원 | P3 | entity-store 사문과 동반 | entity-store 채택 결정 시 함께. |

**공통 선행과제 2개**가 다수를 막음: ① **캐논 연령/로스터 데이터 + 캐릭터 모델 통일**(age-drift·자동진급·coup_conduct),
② **라이브 와이어 RE**(政治·메신저·작전·戦闘艇 sub-action). 둘 다 데이터/RE 작업이라 코드 증분이 아닌 집중 세션 권장.

## 진행 — 닫힘 (2026-06-20 Batch 11 캐릭터모델/자동진급 배선)
- ☑ **P2 자동진급 무유저 배선**: world-state char에 achievement/title(모델 통일) + runMonthlyPromotions +
  createStrategicSim 월간 훅 배선. 大佐이하 사다리 #1 진급. **단 캐논 시나리오는 高계급 14명만 시드라 dormant**
  — 低계급 officer 시드 시 즉시 발화(로직·배선·모델 완비). +2테스트. 서버 **1047테스트**.

누적: **P0 + 7P1 + 15P2 + 11P3**. **남은 건 전부 opcode/데이터/라이브RE/대형refactor 의존**:
- 코드로 가능한 유일 잔여=**A2 codec 추출**(transport codec+login-protocol 빌더→src/server/codec/, re-export shim).
  나머지(age-drift 연령데이터·政治/메신저/작전/戦闘艇 sub-action opcode·coup eval배선·entity-store 설계)는
  데이터복구/라이브RE/아키텍처 결정 선행 필요 → 집중 세션 권장.
- 콘텐츠 후속: 캐논 低계급 officer 로스터를 scenario에 시드하면 자동진급+age-drift(연령 추가 시) 실발화.

## 진행 — A2 codec 추출 착수 (2026-06-20 Batch 12)
- ☑ **transport(child) codec → codec/transport-codec.mjs** 이동 + re-export shim(importer 100% 보존). 순수 이동,
  서버 1047테스트 무변동. codec/ = [transport-codec, base-record, institution-record, warehouse-record,
  personnel-records, scenario-session, simple-info, offsets].
- ⏳ **A2 잔여**: login-protocol.mjs(1969줄)의 코어 빌더(0x0323/0x0325/0x0313/0x0315 + buildStrategicGalaxyGrid)
  → codec/ 이동. **상호의존(그리드 패밀리+buildLobbyResponseInner+STRATEGIC_GRID 상수)이 커 슬라이스가 큼** —
  워크트리 격리 + 패밀리 단위 이동 + 매 이동 테스트 그린 권장(다음 슬라이스부터).
