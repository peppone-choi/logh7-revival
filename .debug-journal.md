# Debug Journal - session registry live lock

Started: 2026-07-01T14:37:00+09:00

Goal: Lock requirements one by one from `docs/logh7-current-requirements-status-2026-07-01.md`, starting with account/session registry behavior.

## Environment snapshot

- Runtime: Node.js server tests plus real Windows LOGH VII `G7MTClient.exe` via `RE/tools/logh7_ui_explorer.py`.
- Entry: `python -m tools.logh7_ui_explorer --session ..\.omo\ui-explorer\session-registry-lock start --server-root ..\server --port 47900 ...`
- Ports: game/auth server `127.0.0.1:47900`.
- References read: `logh7-live`, `logh7-wire`, `logh7-re`, `omo:debugging` methodology setup/investigate/manual QA/native-binary.

## Hypotheses

1. [OPEN] Click/input is not activating the `세션 변경` panel; evidence: repeated `click 575 582` highlights the button but trace has no new event and screenshot stays on blank `서버 공지`.
2. [OPEN] The client parsed `0x2006` but rejected/ignored records; evidence needed: decoded client-side record count/state after `0x2006`.
3. [OPEN] The UI state after login is still the server notice panel, and `세션 변경` needs a different state/coordinate/input path; evidence needed: RE of lobby menu click handler or a proven ui_explorer coordinate.

## Artifacts revert

- [ ] `E:\logh7-revival\RE\.omo` junction -> `E:\logh7-revival\.omo`. Remove only if future tools no longer need the post-restructure bridge.
- [ ] `.omo/ui-explorer/session-registry-lock/` live session directory. Remove after evidence is no longer useful.

- [ ] `RE/tools/logh7_lobby_session_change_probe.py` read-only Frida probe. Keep if it remains useful; otherwise remove after this debug lock.

## Findings

### 2026-07-01T14:37+09:00 - server test lock

- Source: `node --test tests/server/logh7-login-session.test.mjs`
- Value: exit code 0.
- Interpretation: registry-backed `0x2006` list and `0x2009 -> 0x200a` redirect are locked at server-test level.
- Confirms: server side of requirement.

### 2026-07-01T14:37+09:00 - session registry unit tests

- Source: `node --test tests/server/logh7-session-registry.test.mjs`
- Value: 2 pass, 0 fail.
- Interpretation: registry persistence and string-cap tests still pass.
- Confirms: existing registry behavior unchanged.

### 2026-07-01T14:37+09:00 - live client start

- Source: `.omo/ui-explorer/session-registry-lock/server.log`
- Value: `session catalog: E:\logh7-revival\.omo\ui-explorer\session-registry-lock\sessions.sqlite`
- Interpretation: live server used injected `LOGH_SESSION_DB`.
- Confirms: live run exercised registry DB path.

### 2026-07-01T14:37+09:00 - live trace

- Source: `.omo/ui-explorer/session-registry-lock/trace.jsonl`
- Value: `0x2005 -> 0x2006` lobby response sent twice, `respLen:21258`, `frameBytes:21280`.
- Interpretation: client requested session info and server answered.
- Confirms: wire response path reached real client.

### 2026-07-01T14:38+09:00 - live visual

- Source: `.omo/ui-explorer/session-registry-lock/shots/005-session-change-retry2.png`
- Value: `세션 변경` button highlighted; right panel remains blank `서버 공지`.
- Interpretation: not live-verified. Need debug UI activation or client parser/render state.
- Refutes: requirement fully locked at live level.

### 2026-07-01T14:39+09:00 - cleanup

- Source: `python -m tools.logh7_ui_explorer --session ..\.omo\ui-explorer\session-registry-lock stop`
- Value: `shaVerified:true`, no remaining `node`/`G7MTClient` processes.
- Interpretation: live client restored and stopped cleanly.

### 2026-07-01T15:05+09:00 - RE narrowing after user correction

- Source: `python -m tools.logh7_redex func 0x0051a370`, `func 0x00593cf0`, `func 0x00593d90`, `func 0x005946d0`, subagent reports.
- Value: `FUN_0051a370` state `0x15` uses `FUN_00502780(1,4)` + `FUN_005015f0(2, ..., 1)` for the session-change menu, then sets state `0x1c`/`0x20`; state `0x2d` calls `FUN_00593cf0(0)`, state `0x2e` calls `FUN_00593d90`, and `FUN_005946d0` renders picker rows.
- Interpretation: click delivery is no longer the lead hypothesis. The live question is whether the client FSM reaches `0x2d` and whether `0x2006` read-side/selectable gates pass.
- Refutes/Confirms: Refines H1/H2/H3.

### 2026-07-01T15:28+09:00 - original client no-op confirmed
- Source: live Frida probe on canonical playable before patch.
- Value: `FUN_005015f0` returned nonzero for lobby button `group 1:index 4`; `FUN_0051a370` FSM moved `0x17 -> 0x1c -> 0x16`.
- Interpretation: user correction was right. Click was delivered; bug is client FSM branch behavior, not hitbox/coordinate automation.
- Confirms: H1 false as root cause; H3 true.

### 2026-07-01T15:43+09:00 - client patch designed and byte-locked
- Source: `RE/tools/client_patches/session-change-opens-picker.json`.
- Value: VA `0x0051aded`, original `683c677800c7450416000000e8a275070083c404e9570c0000`, patched `e981ffffff9090909090909090909090909090909090909090`.
- Interpretation: Redirects `FUN_0051a370` case `0x1c` to existing case `0x19` picker initializer at `0x0051ad73`, preserving same-length patch bytes.
- Confirms: smallest client-side fix for no-op branch.

### 2026-07-01T15:55+09:00 - patched 1920/remaster live verification
- Source: `.omo/ui-explorer/session-change-picker-1920-live`.
- Value: patched run SHA `8464209aae94d87278016b1e8f3810042ab13dae14384303710e57f9149f4c55`; screenshot `shots/003-patched-1920-session-change.png`; probe recorded `0x17 -> 0x1c -> 0x2d`, `FUN_00593cf0` twice, `FUN_00593d90`, `FUN_005946d0`; row click screenshot `shots/004-patched-1920-session-row.png`; trace recorded `0x2009 -> 0x200a`, then SS reconnect `0x0200 -> 0x0201`.
- Interpretation: 1920/remaster baseline passes the original failing scenario. This is not a 1024-coordinate result.
- Confirms: session-change picker client patch live-verified.

### 2026-07-01T16:06+09:00 - live cleanup
- Source: `python -m tools.logh7_ui_explorer --session ..\.omo\ui-explorer\session-change-picker-1920-live stop`.
- Value: `shaVerified:true`, restored SHA `3b4f634818ff0d2b2f59eb6ddacbe73c9bcbc9cda146b9cfdb9c5d1cb7b98573`.
- Interpretation: installed canonical playable was restored after patched live run.
- Confirms: no patched EXE left deployed by the live session.

### 2026-07-01T16:35+09:00 - canonical playable rebuilt
- Source: `python -m tools.logh7_build_playable_client`.
- Value: `G7MTClient.playable.exe` SHA `272a59d59d5f36dff90facfe431dd5ae5e20c0e886817e74c4e84c64e79bf564`; stack includes `session-change-opens-picker` between `session-select-hardcoded-ko` and `tactical-grid-msgdat-boundaryfix`.
- Interpretation: the live-verified session-change patch is now part of the reproducible canonical playable output.
- Confirms: session registry requirement lock is tied to the default client build, not only an ad-hoc patched EXE.

### 2026-07-01T16:39+09:00 - focused verification
- Source: `python -m py_compile tools\logh7_codepage_patch.py tools\logh7_client_exe.py tools\logh7_build_playable_client.py tools\logh7_lobby_session_change_probe.py`; `python -m unittest tools.tests.test_logh7_client_exe`; `node --test tests/server/logh7-login-session.test.mjs`; `node --test tests/server/logh7-session-registry.test.mjs`.
- Value: py_compile ok; client EXE tests `Ran 9 tests ... OK`; login-session tests `pass 153 fail 0`; session-registry tests `pass 2 fail 0`.
- Interpretation: patch descriptor, default stack, canonical SHA, registry-backed `0x2006`, and `0x2009 -> 0x200a` behavior are regression-locked.
- Confirms: first requirements row can move to `locked: server-tested, client-patch-live-verified`.


2026-07-01T16:47+09:00 - default session picker row locked
- Source: `server/src/server/logh7-session-registry.mjs`, `server/src/server/codec/scenario-session.mjs`, `server/tests/server/logh7-login-session.test.mjs`, `server/tests/server/logh7-session-registry.test.mjs`.
- Value: `DEFAULT_SESSION_RECORDS` is one row (`sessionId:1`, `sessionName:"이제르론 서버"`). `0x2006` packed builder keeps numeric `sessionId` LE but emits session text u16 code units BE.
- RE: `FUN_00444900` remains the packed sequential `0x2006` parser; `FUN_005946d0` is the picker row UI population path. `FUN_00594c20` reads current row fields. Live A/B showed all-u16 BE fixed text but made `0x2009` selected id `256`; mixed id-LE/text-BE fixed both.
- Tests: `cd server && node --test tests/server/logh7-login-session.test.mjs --test-name-pattern "default 0x2006 session catalog|LIVE 0x2005 variant|returns >= 2 selectable|0x2009 maps selected session id|session registry single source"` passed 153/153; `node --test tests/server/logh7-session-registry.test.mjs` passed 3/3.
- Live: `.omo/ui-explorer/session-default-name-final-1920-live` on 1920/remaster canonical playable SHA `272a59d59d5f36dff90facfe431dd5ae5e20c0e886817e74c4e84c64e79bf564`. Screenshot `shots/003-session-change.png` shows one `1 / 1` row named `이제르론 서버`; trace shows `0x2005 -> 0x2006`, row click `0x2009 selectedSessionId=1 sessionAccepted=true`, `0x200a`, then SS `0x0200 -> 0x0201`; stop returned `shaVerified:true`.

### 2026-07-01T16:37+09:00 - default session 0x2009 validation blocker fixed

- Reviewer finding: the no-configured-sessions default path advertised one row via `0x2006` but `0x2009` still accepted arbitrary ids; probe with `selectedSessionId=99` returned `sessionAccepted=true`.
- Fix: `resolveLobbySessionSelection` now treats empty/absent session records as `DEFAULT_SESSION_RECORDS`, so the `0x2009` validator rejects ids outside the advertised default catalog and falls back to session `1`.
- Test: added `login session 0x2009 rejects ids outside default session catalog`; `cd server && node --test tests/server/logh7-login-session.test.mjs --test-name-pattern "default 0x2006 session catalog|outside default session catalog|LIVE 0x2005 variant|returns >= 2 selectable|0x2009 maps selected session id|0x2009 persists selected session id|0x2009 persists only existing open|session registry single source"` passed 154/154. `node --test tests/server/logh7-session-registry.test.mjs` still passed 3/3.

### 2026-07-01T17:45+09:00 - public signup strict login and default session binding locked

- Source: `RE/tools/client_patches/login-blank-password-local-ok.json`, `RE/tools/logh7_client_exe.py`, `RE/tools/tests/test_logh7_client_exe.py`, `server/src/server/logh7-public-account-web.mjs`, `server/src/server/logh7-account-registry.mjs`, `.omo/evidence/row-locks/task-3-public-signup-account-login-2026-07-01.json`.
- RE: `FUN_0051a370` submit path calls `FUN_0051bc20(iVar4 + 0x1304, iVar4 + 0x2028)`. Removed the `0x0051a686` account-pointer rewrite from `login-blank-password-local-ok`; playable SHA is now `375b8adee295be1ffe7b57384f9a4e414d6f7975be2c96bd0dcc2681670c1077`.
- Tests: `cd RE && python -m unittest tools.tests.test_logh7_client_exe` passed 10/10; `cd RE && python -m unittest tools.tests.test_signup_portal` passed 3/3; `cd server && node --test tests/server/logh7-public-account-web.test.mjs tests/server/logh7-signup-first.test.mjs` passed 7/7; session-binding login-session suite passed.

### 2026-07-01T18:45+09:00 - Korean character-name creation and portrait delivery audit

- Source: live sessions `.omo/ui-explorer/task-4-korean-character-bootstrap-live` and `.omo/ui-explorer/task-4-input-probe-live`; evidence `.omo/evidence/row-locks/task-4-korean-character-portrait-audit-2026-07-01.json`.
- Live: canonical playable strict-bootstrap session reached lobby/name form (`0x7000`, `0x2003->0x2004`, `0x2005->0x2006`). Korean create attempt `김`/`한솔`/`백조` did not emit `0x1008`; account DB `characters_json` stayed `[]`.
- Input finding: character-name field accepts hardware ASCII (`shots/033-hardware-ascii-zxcvb-clean.png`) but did not accept cp949 `WM_CHAR`, Unicode `WM_CHAR`, clipboard paste, or Korean IME attempt. Do not treat `WM_CHAR` fallback as a real fix for this form.
- Probe finding: `logh7_message_input_post_probe_patch` at hook `0x0061235a` logged network/message response records (`0x7001`, `0x2001`, `0x2004`, `0x2006`, `0x200a`), not the character-name edit buffer. Next RE target is the custom name-input consumer rather than generic message input.
- Portrait finding: server sends `face`/`portraitIndex` IDs, not portrait image bytes. Missing/blank portrait should be debugged as local `Face/*.tcf` ID mapping/render path after a generated character can be live redisplayed.
- Live: `.omo/ui-explorer/task-3-public-signup-login-fixed-live` used `LOGH_ACCEPT_ANY_GIN7=0`, account DB `.omo/evidence/task-3-public-account.sqlite`, and port `47900`. Trace shows `0x7000 account="task3u01" matchedBy="password"`, `0x2003 -> 0x2004`, `0x2005 -> 0x2006`, `0x2009 selectedSessionId=1 sessionAccepted=true`, `0x200a`, then SS `0x0200 -> 0x0201`; stop returned `shaVerified:true`.
- DB: `task3u01` remains persisted with `selected_session_id=1` and `characters_json=[]`.

### 2026-07-01T19:35+09:00 - Korean edit-widget diagnostic high-byte admission

- Source: `python -m tools.logh7_redex func 0x004066f0`, `func 0x004e7200`, `func 0x004fff60`, `func 0x004ffcd0`, `func 0x00600de9`; live sessions `.omo/ui-explorer/task-4-input-edit-char-movzx-live2` and `.omo/ui-explorer/task-4-input-edit-accept-byteclass-live`; evidence `.omo/evidence/row-locks/task-4-korean-character-input-diagnostic-2026-07-01.json`.
- RE: `0x1008` `CommandGenerateCharacterCharge` parser `FUN_004066f0` reads lastname length/body at `+0x0b/+0x0c`, firstname at `+0x26/+0x28`, and flagship at `+0x60/+0x62` as 16-bit character arrays. Wire payload can carry Korean; the blocker is the local edit widget admission path.
- RE: `WM_CHAR` enters `FUN_004e7200 -> FUN_004fff60 -> FUN_004ffcd0`. `FUN_004ffcd0` appends one-byte ANSI `local_c` through `lstrlenA` helpers. `input-edit-char-movzx` (`FUN_004fff60` byte sign fix) alone did not display Korean or emit `0x1008`.
- Tooling: `RE/tools/logh7_window_login.py` now routes non-ASCII automation text directly to the CP949 `WM_CHAR`/`PostMessageA` path instead of `VkKeyScan`/`keybd_event`; regression test `test_type_text_sends_non_ascii_via_wm_char_path` added.
- Diagnostic patch: `RE/tools/client_patches/input-edit-accept-byteclass-diagnostic.json` changes `FUN_00600de9` final rejected-byte return `xor eax,eax` at `VA 0x00600e6c` to `mov al,1`. This over-accepts rejected bytes and is not safe for default stack.
- Live result: with `input-edit-char-movzx + input-edit-accept-byteclass-diagnostic`, fields rendered Korean: `김` (`shots/012-lastname-text.png`), `한솔` (`shots/014-firstname-text.png`), `백조` (`shots/021-flagship-text.png`). Trace emitted `0x1008` categories `0..3`.
- Not locked: final registration category `4` did not emit; confirm modal click/Enter produced no new trace (`shots/026-confirm-enter.png`). Need narrow DBCS-safe patch and fix/register modal input before claiming Korean character creation complete.

### 2026-07-02 - stale test oracles realigned to BE emit contracts
- Source: `server/tests/server/logh7-scenario-session.test.mjs`, `server/tests/server/logh7-content-pack.test.mjs`.
- Value: 0x2006 oracle str16 now reads u16 BE (id/scalar LE 유지, 2026-07-01 live-verified builder contract); 0x33b count assertion LE->BE (FUN_00421f80 packed 파서 crash-fix 이후 builder BE emit, HEAD 대비 stale test).
- Tests: `cd server && node --test tests/server/*.test.mjs` = 1294 tests / 0 fail.
- Note: `logh7-login-protocol.mjs:1561` 주석은 여전히 "u16 LE count"라고 말해 코드(BE)와 모순 — 0x33b 소비 파서(FUN_004c32a0 vs FUN_00421f80) 재검증 대상.

### 2026-07-03 - Slice 3 lower-left HUD `황제` cache candidate
- Source: `RE/.omo/ui-explorer/slice3-hud-attach-rerun-20260703/`.
- Live: client-created character `Hud` reached world; screenshot `shots/025-world-before-gdi-attach.png` still showed lower-left top label `황제`.
- Wire/state: trace lines 78, 111, 113 emitted `recordDisplayName=Hud`, `recordRank=13`, `recordTitle=""` for `0x0323` and `0x0356`. `player-info-after-gdi.json` proved focus id `16735`, `PLAYER_INFO+0xfa=13`, and `DAT_02227fb8` raw `0000000d`.
- Negative evidence: attach-only GDI captures `gdi-hud-attach.json` and `gdi-hud-rank-hooks.json` did not emit `황제`; they saw date/map text only. Visible lower-left label is not a normal GDI TextOut redraw after HUD is already built.
- Memory evidence: `hwangje-memory-scan.json` found UTF-16LE `황제\n` at `0xbd3db0` with nearby HUD labels/values `PCP`, `MCP`, `100`, `1200`; treat this as strong lower-left HUD cache candidate.
- Tooling: `RE/tools/logh7_frida_gdi_text.py` now has attach-only mode and hooks `FUN_00522010`, `FUN_00597b20`, `FUN_00503560`. From-start `gdi-worldhud-from-start.json` proved `FUN_00503560` hook works, but the one-shot driver still lost the window before the target HUD moment.
- Next: attach before world-entry in the ui_explorer-driven flow, or set an x64dbg/write-watchpoint on the `0xbd3db0` cache candidate. Do not spend another loop rechecking rank/title wire, `PLAYER_INFO+0xfa`, or `DAT_02227fb8` alone.
