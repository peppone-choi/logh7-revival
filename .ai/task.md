# Current Task

- Status: EMPTY — 사람이 승인한 활성 작업 계약 없음
- Goal:
- User value:
- In scope:
- Out of scope:
- Acceptance criteria:
- Constraints:
- Related issue:
- Allowed files:
- Protected files: `.env*`, `*.pem`, `*.key`, `credentials*`, `secrets*`, `terraform.tfstate*` (훅으로 차단됨)
- Required verification: `docs/agent/verification.md`의 변경 유형별 최소 검증
- Human approval checkpoints:

<!--
이 파일은 사람이 작성하거나 승인하는 작업 계약이다.
- 에이전트는 이 파일이 EMPTY일 때 구현을 시작하지 않고, 계약 초안을 제안한 뒤 승인을 받는다.
- 다음 작업 후보는 docs/logh7-roadmap-current.md (M4 선행 게이트 P0→P1→P2)에서 가져온다.
- 승인되면 Status를 ACTIVE로 바꾸고, 완료되면 DONE으로 바꾼 뒤 .ai/handoff.md에 결과를 남긴다.
-->
