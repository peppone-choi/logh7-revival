---
description: LOGH VII Korean localization, font slots, resources, and Korean text cleanup
argument-hint: "[target, e.g. font | menu rsrc | session text | humanize]"
---

Use the **logh7-localize** skill in `.claude/skills/logh7-localize/SKILL.md`. Localize or humanize: `$ARGUMENTS`.

Remember: `String.txt` is cp949, `.rsrc` is UTF-16LE, and the canonical font path uses both the primary UI face slot and the D3D glyph-atlas face slot. Rendering claims require live proof.
