---
description: LOGH VII original-asset extraction and canonical data recovery
argument-hint: "[target, e.g. galaxy positions | portraits | textures | manual]"
---

Use the **logh7-extract** skill in `.claude/skills/logh7-extract/SKILL.md`. Recover: `$ARGUMENTS`.

Inspect/render outputs before trusting a parser. Manual PDF page 101 is the canonical star-chart source; MDX galaxy nodes are templates, not runtime star positions. Tag recovered data with provenance.
